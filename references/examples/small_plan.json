{
  "schema_version": "1",
  "tick_interval_minutes": 5,
  "pool_size": 2,
  "stall_threshold_minutes": 45,
  "launch_grace_minutes": 10,
  "entries": [
    {
      "task_id": "00000000-0000-0000-0000-000000000001",
      "target_repo": "/absolute/path/to/demo-target-a",
      "dispatch_mode": "subagent",
      "worker_prompt": "HEARTBEAT_PATH={HEARTBEAT_PATH}\nTASK_ID={TASK_ID}\nRUN_DIR={RUN_DIR}\nTARGET_REPO={TARGET_REPO}\n\nYou are a demo stub worker (no real work, no API spend). Launch the cross-platform Python demo worker DETACHED so it outlives your turn, then return ONLY a one-line acknowledgment (every path below is substituted mechanically by the harness; do NOT edit or retype any path):\n\n  nohup python3 {HARNESS_BIN}/harness_demo_worker.py --heartbeat-path {HEARTBEAT_PATH} --task-id {TASK_ID} --run-dir {RUN_DIR} --steps 4 --sleep 150 </dev/null >/dev/null 2>&1 &\n\nThe demo worker walks STARTING -> IN_PROGRESS x4 -> COMPLETED via the harness_heartbeat helper. Do NOT read, tail, or echo the heartbeat file; do NOT wait for the background process."
    },
    {
      "task_id": "00000000-0000-0000-0000-000000000002",
      "target_repo": "/absolute/path/to/demo-target-b",
      "dispatch_mode": "subagent",
      "worker_prompt": "HEARTBEAT_PATH={HEARTBEAT_PATH}\nTASK_ID={TASK_ID}\nRUN_DIR={RUN_DIR}\nTARGET_REPO={TARGET_REPO}\n\nYou are a demo stub worker (no real work, no API spend). Launch the cross-platform Python demo worker DETACHED so it outlives your turn, then return ONLY a one-line acknowledgment (every path below is substituted mechanically by the harness; do NOT edit or retype any path):\n\n  nohup python3 {HARNESS_BIN}/harness_demo_worker.py --heartbeat-path {HEARTBEAT_PATH} --task-id {TASK_ID} --run-dir {RUN_DIR} --steps 4 --sleep 150 </dev/null >/dev/null 2>&1 &\n\nThe demo worker walks STARTING -> IN_PROGRESS x4 -> COMPLETED via the harness_heartbeat helper. Do NOT read, tail, or echo the heartbeat file; do NOT wait for the background process."
    },
    {
      "task_id": "00000000-0000-0000-0000-000000000003",
      "target_repo": "/absolute/path/to/demo-target-c",
      "dispatch_mode": "subagent",
      "worker_prompt": "HEARTBEAT_PATH={HEARTBEAT_PATH}\nTASK_ID={TASK_ID}\nRUN_DIR={RUN_DIR}\nTARGET_REPO={TARGET_REPO}\n\nYou are a demo stub worker (no real work, no API spend). Launch the cross-platform Python demo worker DETACHED so it outlives your turn, then return ONLY a one-line acknowledgment (every path below is substituted mechanically by the harness; do NOT edit or retype any path):\n\n  nohup python3 {HARNESS_BIN}/harness_demo_worker.py --heartbeat-path {HEARTBEAT_PATH} --task-id {TASK_ID} --run-dir {RUN_DIR} --steps 4 --sleep 150 </dev/null >/dev/null 2>&1 &\n\nThe demo worker walks STARTING -> IN_PROGRESS x4 -> COMPLETED via the harness_heartbeat helper. Do NOT read, tail, or echo the heartbeat file; do NOT wait for the background process."
    }
  ]
}
