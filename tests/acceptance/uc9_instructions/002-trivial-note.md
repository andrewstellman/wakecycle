# UC-9 in-context task 002 (trivial)

The SECOND in-context queue item. UC-9's load-bearing leg: after the session is
killed and rehydrated in a FRESH context (FR-48 ERR discipline), the orchestrator
resumes the IN-CONTEXT QUEUE here (task 002) - not just the background run-dir.
Trivial and zero-spend: write a one-line note to `outputs/002-trivial-note.md`,
then the queue is drained. STOP at the queue root halts selection, even mid-queue.
