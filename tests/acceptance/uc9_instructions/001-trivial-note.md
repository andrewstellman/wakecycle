# UC-9 in-context task 001 (trivial)

An in-context queue item the orchestrator does ITSELF, in its own context,
between background ticks (FR-46/47). Trivial and zero-spend: write a one-line
note to `outputs/001-trivial-note.md` saying the task was processed, then move
to the next queue item. The background harness (`uc9_background.json`) keeps
running independently at any rung.
