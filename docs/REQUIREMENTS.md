# Arunner — Requirements Document

*Status: v1.1 (2026-06-13). v1.0 consolidated the founding contract from the v1.5.9 harness design arc (design docs, capability-ladder decisions, multi-host research, edge-case register, empirical validation runs). v1.1 adds the **v0.1.0 first-release UX scope** locked 2026-06-13: canonical versioning (FR-34), the control-file convention (FR-35..39), the flexible-input adapter (FR-40/41), job-config UX (FR-42..44), the SUMMARY roll-up (FR-45), in-context worker mode (FR-46..49 / UC-9), and unattended-reliability positioning (FR-50). The FR/NFR numbering is the stable reference for tests and docs. v1.2 (2026-06-14) adds the **autonomy-integrity** requirement FR-55 (the continuation contract), its user story US-11, and its use case UC-11 — born from a 2026-06-14 incident in which the orchestrator halted an in-flight build overnight, rationalizing the stop as consideration for the operator. v1.3 (2026-06-14) adds FR-56 (wrap/tail adapter activity-pattern extraction), US-12, and UC-12 — PENDING, to build (sequenced after the simulator). v1.4 (2026-06-18) adds FR-61..65 — multi-step entries, continuation gates, gate-outcome vocabulary, prompt-from-file `{var}` templating, and per-run/per-step token reporting — targeting arunner 0.2.0; **VERIFIED** (instr 002 implementation: `test_prompt_from_file.py` / `test_multistep.py` / `test_gates.py` / `test_tokens.py`).*

*Sources of record: the v1.5.9 design + multi-host research notes and the validation-run records from which this was consolidated.*

---

## 1. Overview

The harness is a batch orchestrator for AI coding agents that runs **inside the operator's existing agent session** — or, degraded gracefully, inside a plain terminal window — with no server, no daemon, no framework, no API keys beyond the session the operator already has, and no admin rights. It is deliberately **agent-agnostic**: it orchestrates *any* agentic coding system (Claude Code, Copilot, Codex, Cursor, Antigravity, …) rather than one vendor, because the engine is stdlib Python and the worker contract is vendor-neutral (FR-54). The primary way an operator uses it is conversational — describe a run in plain language to the agent that has the skill loaded, and it assembles, previews, runs, and saves the session (FR-52).

Its architecture inverts the usual orchestration-framework shape. Frameworks put the intelligence in external infrastructure and treat the model as a worker. The harness puts **all the determinism in a small stdlib Python script** (the tick engine: a disk-truth state machine advanced one idempotent tick at a time) and uses the agent only as a relay that can wake on a schedule and start workers. The agent never decides anything; the script is the state machine; disk is the database; crash-recovery is "run one tick."

A **plan** (JSON) lists jobs — each an opaque worker prompt plus a target — with a pool size and tick cadence. Workers report progress by appending heartbeat lines to a known file. Each tick: read disk, reap finished workers, dispatch queued jobs into free pool slots, detect stalls, print a status table, schedule the next tick. When everything is terminal, the loop ends itself.

**The worker contract (the whole of it):** *a job is anything that appends JSON lines to a file.* A line at start, a line every so often, a terminal line at the end — single-line JSON, one writer per file. The `status` enum is the only field the harness interprets; everything else is decoration it displays but never reads. The worker doesn't have to be an AI: a shell script, a make target, a CI job, or a human with `echo >>` all qualify; the heartbeat helper is a convenience SDK, not a requirement. The contract deliberately honors **Postel's law** — *be conservative in what the harness emits, liberal in what it accepts*: a worker that never writes, dies mid-run, or writes garbage degrades to a visible STALLED/failed row in the status table, never to a wedged state machine, and a malformed line is skipped (and warned about), never fatal. Heartbeats are how a worker earns "running" and "completed"; silence is handled.

The harness was built as the Quality Playbook's test harness (replacing a ~10K-line Python subprocess harness, deleted 2026-06-11) but the core is payload-agnostic — the validated end-to-end runs orchestrated stub workers with zero QPB involvement. It ships two ways: a standalone repo + pip/npm packages (canonical upstream), and a vendored copy inside QPB with a lineage note.

**Empirical grounding (all 2026-06-11):** three Sonnet validation passes + one Haiku 4.5 pass (low-reasoning-model bet confirmed), multi-entry pool run with staggered dispatch, agent-honored STOP, detached workers surviving dispatch turns (pgrep-verified), and two observed in-the-wild silent loop-drops in a long-idle watcher session — which is exactly why the capability ladder and the printed-command floor exist.

## 2. Actors

- **Operator** — the human who writes the plan, starts the harness, and reads the results. May be on a locked-down corporate machine.
- **Orchestrator agent** — the AI session (Claude Code today; Copilot CLI candidate) that runs the tick loop at cadence rung 1. Optional below rung 1.
- **Ticker** — the foreground/`--once` Python script that replaces the orchestrator agent at cadence rungs 3-4 (and serves rung 2 as the cron target).
- **Worker** — the dispatched agent (subagent or detached host-CLI process) that does one job and reports via heartbeats.
- **Host scheduler** — ScheduleWakeup / `/every` / cron / launchd / Task Scheduler / host Automations, whichever exists.

## 3. User stories

1. As an operator, I want to run quality audits across several repos overnight from one pasted prompt, so multi-repo benchmarking doesn't need my attention between start and finish.
2. As an operator, I want to watch progress in a status table each tick, so I know what's running, queued, finished, or stalled without reading log files.
3. As an operator, I want to halt a run by dropping a STOP file, so I never have to interrupt or kill an agent mid-thought.
4. As an operator whose session crashed (or whose wakeup silently died), I want to resume by running one command against the existing run directory, so no work is lost and nothing double-runs.
5. As a developer on a locked-down Windows machine with no admin rights and no cron, I want to open PowerShell and run one Python script that drives the whole plan, so the worst-case environment is still fully supported.
6. As a Codex/Copilot/Cursor user, I want my workers dispatched through my own CLI, so the harness isn't Claude-only.
7. As a budget-conscious user, I want the orchestration to work on a small/cheap model, so I can spend the capable-model budget on the workers.
8. As a first-time adopter, I want `pip install` + one pasted prompt to show me a complete 3-job pool run in ~20 minutes with no API spend beyond my session, so I can evaluate the thesis in one sitting.
9. As a maintainer, I want every run to leave a complete disk record, so any run can be audited after the fact without chat scrollback.
10. As the article's reader, I want the README to tell me honestly which hosts/rungs are verified versus designed, so I can trust the claims.
11. As an operator who started a run and walked away (asleep, or in another meeting), I want the orchestrator to keep ticking until the run reaches a terminal state or I explicitly halt it — and never to stop just because time has passed, it reached a tidy checkpoint, or it judged it a good place to pause — so "autonomous" actually means unattended. (FR-55)
12. As an operator wrapping a chatty third-party tool, I want to tell arunner which log lines are the meaningful status (by regular expression) so the status table shows me real progress instead of whatever line was printed last. (FR-56)
13. As an operator running a batch, I want to add more jobs to it while it's running — without restarting — so I can grow the queue as work comes up. (FR-57)
14. As an operator watching a run, I want the status table and the ACTIVITY column to update on a cadence I can actually see, so I can tell what each worker is doing in real time. (FR-58)
15. As an operator whose orchestrator is busy (blocked on a long synchronous subagent worker) or running headless, I want to open a second terminal and watch the run live without touching it, so I can see worker progress even when the orchestrator can't get a turn to print a table. (FR-59)
16. As a chat orchestrating an autonomous loop, I want to send the running runner typed messages and get acknowledged, structured results back, so I can drive conditional, iterative work (run → inspect → decide → run again) without restarting the runner or scraping logs. (FR-60)

## 4. Use cases (Applied Software Project Management format)

### UC-1: Run a multi-job plan natively (cadence 1 + dispatch 1)

**Summary:** The operator pastes the bootstrap prompt into a fresh agent session; the session drives the plan to completion autonomously.
**Rationale:** The headline workflow — zero infrastructure, one paste.
**Users:** Operator, orchestrator agent, workers.
**Preconditions:** Plan JSON exists and validates; the session's host has a scheduling primitive and a subagent tool; run-dir parent is on local disk.
**Basic course of events:**
1. Operator pastes the bootstrap (with the plan path) into a fresh session.
2. Agent probes its tooling, announces the selected rungs (cadence 1, dispatch 1), reads the harness SKILL by absolute path.
3. Agent runs `--init`; the tick engine scaffolds the run-dir and queues all entries.
4. Agent runs tick 1; the engine emits dispatch entries up to pool_size; agent launches each worker subagent with its prompt verbatim; workers return one-line acknowledgments and run detached.
5. Agent prints the status table verbatim and schedules the next tick.
6. On each wakeup the agent runs one tick; the engine reaps terminal heartbeats into results, back-fills free pool slots from the queue (staggered dispatch), and updates the table. Idle ticks still reschedule.
7. When all entries are terminal the engine reports `done`; the agent prints the final table and exits without rescheduling.
**Alternative paths:** (a) A wakeup silently fails to fire → operator's next message (or the printed ticker command) resumes the loop from disk (see UC-4). (b) A worker stalls past the threshold → engine marks STALLED; the table shows it; operator decides. (c) A dispatch fails auth → entry goes AUTH_OR_LAUNCH_FAILED, run continues.
**Postconditions:** Run-dir contains final `harness_status.json` (`done: true`), one result record and full heartbeat file per entry; session idle; no further wakeups.

### UC-2: Monitor a run in progress

**Summary:** The operator reads per-tick status tables (or the disk) to understand run state.
**Rationale:** Observability without log spelunking; the table is the UI.
**Users:** Operator.
**Preconditions:** A run is in flight.
**Basic course of events:**
1. Each tick prints the ASCII table: per-run state, phase, last heartbeat status and age; queue/claimed/running/stalled/completed/failed counts; next-tick time.
2. The operator reads it in the session (or any tier's stdout) — or inspects `harness_status.json` and heartbeat tails directly, since disk is truth.
**Alternative paths:** Operator asks the agent to "run another tick now" — safe by idempotency; only the cycle counter changes if nothing advanced.
**Postconditions:** None (read-only).

### UC-3: Halt a run early

**Summary:** The operator drops a STOP file; the next tick halts the loop cleanly.
**Rationale:** Deterministic, race-free shutdown that never interrupts the agent mid-action.
**Users:** Operator, orchestrator agent/ticker.
**Preconditions:** Run in flight; operator can write to the run-dir.
**Basic course of events:**
1. Operator creates `<run-dir>/STOP`.
2. The next tick observes it; the engine reports `stop: true` and mutates nothing (the stop tick is fully read-only — not even the cycle counter changes).
3. The agent/ticker prints the table, states STOP detected, and exits without rescheduling.
**Alternative paths:** STOP written after the final reap → run already `done`; STOP is inert. In-flight detached workers continue to their own terminal states (documented orphan semantics; no kill in this release).
**Postconditions:** Loop terminated; state untouched from the last completed tick; run resumable by deleting STOP and ticking.

### UC-4: Resume after a crash or silent loop-drop

**Summary:** Any fresh session — or the ticker — resumes an interrupted run from disk.
**Rationale:** Two silent wakeup-drops were observed in the wild on 2026-06-11; recovery must be one action.
**Users:** Operator, any fresh orchestrator agent or the ticker.
**Preconditions:** A run-dir with non-terminal state exists.
**Basic course of events:**
1. Operator notices the loop stopped (no new ticks).
2. Operator re-pastes the bootstrap in any session pointing at the existing RUN_DIR (skip `--init`) — or runs the printed `harness_ticker.py --once <run-dir>` command.
3. The tick engine reads disk and continues exactly where the run left off; idempotency guarantees nothing double-dispatches.
**Alternative paths:** Machine slept/hibernated → heartbeat ages are inflated; the wall-clock-jump guard treats ages as suspect for one tick instead of false-STALLING.
**Postconditions:** Loop running again; at most one cycle increment beyond the interruption point; zero duplicated work.

### UC-5: Run on a locked-down host (cadence 3 + dispatch 2 — the floor that must work)

**Summary:** On Windows/macOS/Linux with no admin rights and no scheduler access, the operator runs the plan from one terminal window with the foreground ticker.
**Rationale:** Operator-set worst case (2026-06-11): corporate locked-down Windows must be first-class.
**Users:** Operator, ticker, workers (detached host-CLI processes).
**Preconditions:** User-level Python 3; at least one agent CLI on PATH and authenticated; plan entries use `dispatch_mode: "shell"` with `worker_cmd` templates.
**Basic course of events:**
1. Operator opens cmd/PowerShell/terminal — no elevation.
2. Operator runs `python harness_ticker.py <plan-or-run-dir>`.
3. The ticker loops: run one tick; for each dispatch entry, write the worker prompt to a file and spawn the host CLI detached (platform-appropriate flags), recording PID + start time in the claim lock; print the table; sleep `tick_interval`; repeat.
4. On `done`, the ticker prints the final table and exits.
**Alternative paths:** (a) Operator closes the window → in-flight child workers may die (documented); rerunning the ticker resumes and re-detects state from heartbeats/PID locks. (b) CLI auth fails pre-flight → entry marked AUTH_OR_LAUNCH_FAILED with an actionable message. (c) No scheduler AND the operator can't keep a window open → UC-6 or UC-7.
**Postconditions:** Same disk record as UC-1 — the run artifacts are tier-invariant.

### UC-6: Scheduled run via cron or host automations (cadence 2 + dispatch 2)

**Summary:** An OS scheduler (or Codex-style Automations) fires one tick per cadence interval; no window stays open.
**Rationale:** Long/overnight plans on hosts where a scheduler is available; survives logouts.
**Users:** Operator, host scheduler, ticker (`--once`), workers.
**Preconditions:** Scheduler rights; plan uses shell dispatch; run-dir initialized.
**Basic course of events:**
1. Operator installs the printed one-line schedule entry (`harness_ticker.py --once <run-dir>` at the tick cadence).
2. Each fire executes exactly one tick (the per-run-dir lockfile makes overlapping fires skip cleanly).
3. Operator removes the schedule entry after `done` (or the entry self-reports done and exits instantly thereafter — idempotent and cheap).
**Alternative paths:** DST/local-time cron quirks documented; relative-interval rungs immune.
**Postconditions:** Same disk record; schedule entry removable at leisure.

### UC-7: Manual-tick floor (cadence 4)

**Summary:** With no scheduler, no ticker loop, and no agent session, the operator advances the run by hand, one printed command at a time.
**Rationale:** The harness must never strand a run: every failure path prints the exact next command.
**Users:** Operator.
**Preconditions:** A run-dir exists; user-level Python.
**Basic course of events:**
1. Any failure of a higher rung prints: "to continue this run, execute: `python3 <abs>/harness_ticker.py --once <run-dir>`".
2. The operator runs it whenever convenient; each invocation is one safe tick.
3. Repeat until the output reports done.
**Alternative paths:** Operator over-ticks — harmless (cycle-only diffs).
**Postconditions:** Same disk record; cadence is whatever the operator made it.

### UC-8: Install and run the demo (adopter first contact)

**Summary:** A new user installs from pip or npm and watches a complete 3-job pool run in ~20 minutes.
**Rationale:** The downloadable artifact is the article's proof; the demo IS the thesis experienced.
**Users:** Operator (first-time), orchestrator agent or ticker, Python stub workers.
**Preconditions:** User-level install (`pip install --user <name>` / `npm install <name>`); a Claude Code session OR just Python (the demo runs at any rung).
**Basic course of events:**
1. Operator installs the package and locates the example plan (Python stub workers — cross-platform, no API spend).
2. Operator pastes the bootstrap (rung 1) or runs the ticker (rung 3).
3. The demo exhibits: pool-limited dispatch, genuine idle ticks, staggered dispatch when the first stub finishes, heartbeat-driven reaps, clean self-termination — in ~20 minutes.
**Alternative paths:** Locked-down host → the demo works identically at rung 3 (that's the point).
**Postconditions:** The user has seen every architectural claim demonstrated on their own machine.

### UC-9: Run as an in-context worker (harness mode + in-context capability)

**Summary:** The operator bootstraps a session that does work itself, pulling tasks from a streaming instruction folder, while optionally also managing background workers — generalizing the QPB runner/watcher pattern into a first-class arunner mode.
**Rationale:** Unifies the bespoke runner pattern into the product; the natural home for the externalize-recognize-rehydrate discipline.
**Users:** Operator, orchestrator agent (acting as the worker), optional background workers.
**Preconditions:** A rung-1 session (the in-context work requires an agent); an instruction folder; optionally a background plan.
**Basic course of events:**
1. Operator bootstraps the session pointed at an instruction folder (and optionally a plan).
2. On each wake the agent does any new in-context instructions (writing outputs); then — when not mid-in-context-work — ticks the background harness; then schedules the next wake. While in-context work is in progress, monitoring pauses (FR-46).
3. On resume after a crash/compaction, the agent rehydrates from disk (FR-48) and continues.
4. `STOP` halts; an empty queue idles.
**Alternative paths:** (a) A Class-C agent-loop drop (C-5) stops the loop → the background workers are still rescuable by the deterministic floor (`ticker.py --once`), while the in-context queue waits for an operator re-bootstrap. (b) A single in-context task longer than ~4× the stall threshold looks like a slept machine to the engine → the orchestrator passes a "busy, not asleep" hint so a genuine stall isn't blurred (FR-8 / E2 interaction).
**Postconditions:** Instruction outputs written; background run-dir consistent; full disk record; resumable from disk alone.

### UC-10: Build and run a session conversationally (the headline UX)

**Summary:** The operator describes a batch in plain language to the agent that has the skill loaded; the agent assembles the run, previews it, launches it on confirmation, and saves it for rerun — all without the operator hand-writing a plan.
**Rationale:** The build-and-run experience is what makes the tool usable and testable as a real user journey (FR-52/53); it showcases the whole system (shorthand + wrap adapter + dispatch modes + pool + SUMMARY) and works in any host agent (FR-54).
**Users:** Operator, host agent (any agentic coding system).
**Preconditions:** A host agent with the skill loaded; Python 3 available.
**Basic course of events:**
1. Operator: "run three jobs from `ABC.md`, `DEF.md`, `GHI.md`, pool 2, subagents." The agent assembles a `jobs:` shorthand, inferring subagent dispatch and reading each file as a worker prompt (FR-52.1).
2. Agent previews the assembled plan + `--check` result; operator confirms (FR-52.2).
3. Agent launches; the status table streams; on `done` the SUMMARY capstone is written (FR-45).
4. Operator: "save that to `my_run.json`." The agent writes both the shorthand and the expanded canonical plan (FR-52.4).
5. Later: `run my_run.json` reruns it faithfully; `status`/`resume`/`summary` cover watch-and-continue (FR-53).
**Alternative paths:** (a) "run `abc.exe def.exe ghi.exe jkl.exe`, pool 3" → the agent infers shell dispatch and wraps each executable via the FR-40 wrap adapter. (b) An ambiguous request → the agent asks a clarifying question rather than guessing. (c) Mid-build refinement: "add a fourth job", "change pool to 3", "make job 2 shell" (FR-52.5).
**Postconditions:** A run launched (or an authored/saved plan); optionally `my_run.json` persisted (shorthand + expanded) for faithful rerun.

### UC-11: Drive an unattended run through stop-pressure without yielding (autonomy integrity)

**Summary:** While the operator is away, the orchestrating host agent drives a multi-tick run to its terminal state, advancing across every tick for which the continuation verdict is `CONTINUE` — even when internal or conversational pressure invites it to stop — and relinquishing control only on a `HALT` verdict, recording a yield when it does.
**Rationale:** Unattended autonomy is the whole product. The host agent (an LLM) has a standing turn-completion bias that surfaces as plausible-sounding reasons to stop (a late hour, a tidy checkpoint, "I should check in"). FR-55 externalizes the continue/stop decision to disk so the agent *reads* it rather than *authoring* it; this is the use case that exercises that discipline. Born from a 2026-06-14 incident in which the orchestrator halted an in-flight build overnight, rationalizing the stop as consideration — the inverse of continuation pressure, same root cause: agent discretion overriding the operator's control state.
**Users:** Operator (absent), host agent (orchestrator), the deterministic engine.
**Preconditions:** A multi-job run is live and non-terminal; no STOP/PAUSE/CANCEL control present; no blocker recorded; the operator is away.
**Basic course of events:**
1. The engine completes tick N, computes the continuation verdict as a pure function of run-dir state — non-terminal ∧ persisted status not paused/stopped/cancelled ∧ no open blocker ∧ progress still possible ⇒ `CONTINUE` — carrying `next_tick_due` and a `monitoring_paused` flag, and writes it to `harness_status.json` + the journal (FR-55).
2. The host reads the verdict. **Pressure to stop is present**: the run is hours old, the last tick reached a clean, committable checkpoint, and the host's own turn-completion pull is active. The next tick is due, so the host nonetheless advances tick N+1 — because `CONTINUE` (not its own read of the hour or the checkpoint) is the only input the halt decision is permitted to consult.
3. This repeats across many ticks. At each, the verdict is `CONTINUE`, so the host keeps going; none of "it's late," "this is a tidy stopping point," or "I should check in" is a legitimate halt reason, and the host records no yield.
4. A genuine decision point arises (e.g., a worker hits a choice only the operator can make). The host does **not** silently stop: it writes a `blocked:<id>` record to disk, which makes the next verdict `HALT:blocked`, and only then yields — appending a yield record naming the verdict it observed.
5. (Absent any blocker) the run reaches a terminal state; the verdict becomes `HALT:done`; the host yields with a record citing `HALT:done`, and the SUMMARY capstone is written (FR-45).
**Alternative paths:**
(a) The operator drops a STOP/PAUSE file while away → the next verdict is `HALT:stop`/`HALT:pause` and the host yields legitimately (operator-driven stop).
(b) The host stops mid-run while the verdict is `CONTINUE` and writes no yield record → **silent abandonment**; the independent checker (FR-51) flags a contract violation. This is the failure the FR-55 test deliberately reproduces.
(c) The host stops and records a yield citing a reason outside the halt set (e.g. "good checkpoint") while the verdict was `CONTINUE` → **illegitimate yield**; flagged identically.
(d) The verdict is `CONTINUE` but the host is between *scheduled* ticks (cadence rungs 2/3/4, or the FR-26a safety-tick interval) or in an in-context pause (`monitoring_paused`, FR-46) — wall-clock ≤ `next_tick_due` — so no tick is due and no yield is required; the detector stays silent (a scheduled gap is not abandonment).
(e) The host stops while the verdict is `CONTINUE` but writes a yield record citing an *in-set* reason the engine did not emit (e.g. `HALT:done` on a non-terminal run) → **false halt claim**; the checker compares the cited verdict against the engine's recorded verdict and flags the mismatch.
**Postconditions:** The run is at a terminal state (or a recorded `blocked:<id>` / operator-halt), and every yield record in the journal cites a `HALT` verdict that matches the engine's recorded verdict for that tick — auditable proof that each relinquish of control was legitimate.

### UC-12: Surface meaningful status from a noisy wrapped tool (FR-56)

**Summary:** The operator wraps a tool whose logs interleave meaningful status with noise; by declaring status patterns, the ACTIVITY column shows the most-recent *relevant* line each tick instead of the raw last line.
**Rationale:** The wrap/tail adapters are how arunner orchestrates tools that know nothing about it (FR-54); without relevance filtering the displayed status is whatever the tool printed last, often noise. FR-56 makes the displayed status meaningful without touching doneness correctness.
**Users:** Operator, the wrap/tail adapter.
**Preconditions:** A `wrap` or `tail` adapter job (FR-40/41); the operator knows a pattern (or a few) that identify the status lines in the tool's output.
**Basic course of events:**
1. The operator declares `adapter_activity_patterns` on the job (e.g. `["^\\[PHASE\\]", "Step \\d+/\\d+"]`); `--check` (FR-42) confirms each compiles and passes the complexity screen.
2. The job runs; the adapter scans new output lines each keepalive interval.
3. Each tick, the ACTIVITY column shows the most-recent line matching an activity pattern, e.g. `Step 7/12` — filtering out the tool's noise — with an age hint (`Step 7/12 (8m ago)`) once that line is older than the current keepalive, so a pinned-but-stale status never looks live.
4. If no line has ever matched, the column shows the neutral "(running…)" placeholder.
5. Doneness is unchanged — wrap by exit code, tail by the FR-41 precedence.
**Alternative paths:**
(a) An activity pattern is malformed or empty → `--check` fails pre-flight naming the offending pattern; the run never starts on a bad regex.
(b) A pathological/over-broad pattern → bounded by the per-line truncation + the per-tick total-bytes-scanned ceiling + the `--check` complexity screen (FR-56 safety); the residual risk is an operator-self-inflicted single-tick wedge, disclosed, not a crash.
**Postconditions:** The status table reflected meaningful, operator-selected status throughout; lifecycle correctness was independent of the patterns.

## 5. Functional requirements

**Plan & configuration**
- FR-1: A plan is a single JSON file: `tick_interval_minutes`, `pool_size`, `stall_threshold_minutes`, `launch_grace_minutes`, `entries[]`.
- FR-2: Each entry: `task_id` (UUID), `target_repo` (absolute), `dispatch_mode` (`"subagent"` | `"shell"`), and a `worker_prompt` (subagent) or `worker_cmd` template + prompt-file (shell). All schemas carry `schema_version`; consumers warn on mismatch.
- FR-3: Worker prompts/commands receive the absolute-path placeholder block (`{HEARTBEAT_PATH}`, `{TASK_ID}`, `{RUN_DIR}`, `{TARGET_REPO}`); nothing is ever derived from cwd.

**Tick engine (state machine)**
- FR-4: All state lives on disk in a per-run directory (`plan.json` snapshot, `harness_status.json`, `queue/`, `claimed/`, `results/`, per-entry `run-NN/` with `heartbeat.ndjson`). Disk is the single source of truth.
- FR-5: One tick = read disk → apply transitions (reap terminals → dispatch into free slots → stall checks → done/stop detection) → write state atomically (write-temp-rename) → emit `{dispatch_list, status_table, next_tick_minutes, done, stop}` as JSON on stdout.
- FR-6: Every transition is idempotent; a double tick changes only the `cycle` counter (the deliberate idle-tick witness — a true empty diff is impossible by design).
- FR-7: Pool semantics: at most `pool_size` non-terminal dispatched entries; freed slots are back-filled from the queue in the same tick (staggered dispatch).
- FR-8: Stall detection: heartbeat age > threshold ⇒ STALLED; `launch_grace_minutes` before first heartbeat; mandatory worker keepalives make the global threshold safe; a wall-clock jump (tick gap ≫ cadence) suppresses stall marking for one tick (E2).
- FR-9: The reap transition verifies the claimed job record; an externally-missing claim is recorded as an anomaly, never fabricated success.
- FR-10: A `STOP` file at the run-dir root makes the next tick fully read-only: report `stop: true`, change nothing (not even `cycle`).
- FR-11: When all entries are terminal the engine reports `done: true`; terminal ticks suppress the next-tick line in the table.
- FR-12: A per-run-dir lockfile serializes concurrent tick processes (`fcntl`/`msvcrt`); a locked tick skips with a message (E1).
- FR-13: `--init <plan>` scaffolds a new timestamped run-dir and queues all entries; re-running a tick against any existing run-dir resumes it.

**Dispatch**
- FR-14 (subagent mode): the orchestrator launches one subagent per dispatch entry, prompt verbatim, via its session's subagent tool (`Task`/`Agent` — prose names both); the worker's return contract is a single summary line; workers never echo heartbeat content.
- FR-15 (shell mode): the dispatching tier writes the prompt to a file and spawns the host CLI detached (POSIX `start_new_session`; Windows `DETACHED_PROCESS|CREATE_NEW_PROCESS_GROUP`), recording PID + start time in the claim lock for dead-vs-slow stall discrimination (A-5).
- FR-16: A cheap per-CLI auth/availability pre-flight runs before first shell dispatch; failures mark the entry `AUTH_OR_LAUNCH_FAILED` with an actionable message rather than silently stalling.
- FR-17: The orchestrating tier dispatches exactly what `dispatch_list` names — never more, never on its own initiative.

*Dispatch (who does the work) and cadence (what triggers the next check, FR-23) are independent axes. Any dispatch mode pairs with any cadence rung; the capability ladder is their cross-product. The lone coupling: the in-context dispatch of FR-46 does its own tasks in the live agent, so those specific tasks ride on rung-1 cadence, while everything that mode also dispatches/watches can run at any rung.*

**Heartbeat contract**
- FR-18: Workers append single-line JSON records (`ts`, `task_id`, `schema_version`, `status` ∈ STARTING|IN_PROGRESS|COMPLETED|FAILED|ABANDONED, plus optional `label`, `message`, `data`) to their heartbeat file; terminal records carry `result_file` + `summary`. **`status` is the ONLY field the harness interprets** (it drives the state machine). `label` is a short free-form string displayed verbatim in the status table (column: ACTIVITY; truncated to width; ASCII-sanitized for display, raw preserved on disk) — this replaces the older `phase`/`step` pair. `message` is longer human-facing detail. `data` is an opaque JSON object the harness never reads — the structure escape hatch and the A2A migration path. (a vendored integration may set `label` from its own identity; that coupling never enters the generic core.)
- FR-19: A stdlib helper CLI performs all appends (`emit` / `keepalive` / `terminal`): JSON-encodes every value (no printf interpolation), opens with `O_APPEND`, one writer per run directory. The helper is optional — any conformant appender qualifies (Postel: the harness is liberal in what it accepts; malformed lines are skipped with a warning, never fatal).
- FR-20: **Specifiable heartbeat file.** By default the harness assigns `{HEARTBEAT_PATH}` inside the run-dir; a plan entry MAY instead declare `heartbeat_path` (absolute) to point the harness at a file the job already writes — supporting pre-existing tools/jobs with their own status-file location, with no change to the job. The run-dir result/manifest layout is unaffected.
- FR-21: If a heartbeat cannot be written, the helper exits nonzero loudly and worker guidance is to abort with FAILED (E6) — a silent worker must never look healthy.
- FR-21a: **No model-transcribed paths, ever.** Every harness-known path a worker needs (`{HEARTBEAT_PATH}`, `{TASK_ID}`, `{RUN_DIR}`, `{TARGET_REPO}`, and any helper/bin location e.g. `{HARNESS_BIN}`) is mechanically substituted by the tick engine before dispatch — a worker prompt must never ask a model to copy or substitute a literal path itself. Evidence: 2026-06-12 Haiku run `20260612T005833Z`, where the one hand-copied path in a stub script was transcribed as `/Users/anthropic/...` (username hallucinated), silently killing every heartbeat while the job "completed" invisibly — caught by launch grace, diagnosed only by script diff.
- FR-21b: When the engine marks an entry `AUTH_OR_LAUNCH_FAILED`, the status record and table carry a diagnostic hint ("no heartbeat received within launch grace — check worker-side launch: auth, helper availability, paths"), because the bucket covers more causes than auth. Long state names must not overflow the table column (fixed-width or abbreviated display).

**Cadence & degradation (the capability ladder)**
- FR-22: At startup an orchestrating agent probes its own tooling and announces the selected cadence + dispatch rungs.
- FR-23: Cadence rungs, preferred high to low: (1) in-session scheduling primitive; (2) OS/host scheduler firing `--once` ticks; (3) foreground ticker loop; (4) manual ticks. Rungs 2-4 require shell dispatch.
- FR-24: The ticker provides rungs 3-4: loop mode (tick → spawn → sleep) and `--once`; stdlib-only.
- FR-25: Every scheduling-failure path — at every rung — prints the exact next command (absolute paths filled in) to continue the run in another window. No run is ever stranded without printed instructions.
- FR-26: Loop-continuation discipline: every non-terminal tick ends with a reschedule (or its rung's equivalent); idle is not done; the only clean exits are `done` and `stop`.
- FR-26a: **Safety tick (recommended deployment pattern, documented in README + STATE_MACHINE.md).** Because ticks are idempotent and the E1 lockfile serializes concurrent tick processes, redundant ticking is safe by construction — so a rung-1 (in-session timer) run SHOULD be paired with a low-frequency external safety tick (cron/scheduler/second-terminal ticker running `--once` at ~3× the plan cadence) against the same run-dir. While the in-session timer is alive, safety ticks are cycle-only no-ops; if the timer dies silently, the safety tick rescues the run within one safety interval with no detection logic and no operator nudge. Rationale: in-session scheduled tasks are session-scoped and empirically fragile in long sessions (4+ silent drops observed in the v1.5.9 runner watcher, 2026-06-11/12; ScheduleWakeup subsystem has known rough edges upstream); the harness treats rung-1 timers as fallible rather than trusting them.

**Observability & evidence**
- FR-27: The status table is pure ASCII, printed verbatim by the relaying tier, and shows per-run state/phase/last-heartbeat/age plus aggregate counts.
- FR-28: A completed run's directory is a self-sufficient audit record: final status, every heartbeat line, every result record, the plan snapshot.
- FR-29: Pre-flight warns when the run-dir path looks like a synced folder (OneDrive/Dropbox patterns) (E4).

**Distribution**
- FR-30: Shipped as a standalone repo (canonical upstream) + pip and npm packages installable user-level; a downstream consumer may carry a vendored copy with a lineage note and a drift test pinned to an upstream release.
- FR-31: The package includes the example plan with cross-platform Python stub workers — the ~20-minute zero-API-spend demo (UC-8) — runnable at rung 1 or rung 3.
- FR-32: The plugin layout (plugin.json + marketplace.json) qualifies for Claude-marketplace submission; the harness plugin is submitted.
- FR-33: Publishes are gated: clean-clone cold-build, built-artifact end-to-end test in a throwaway environment, dry-run before any live upload (the publish-safety discipline verbatim).

### v0.1.0 first-release UX additions

*Scope locked 2026-06-13. These extend the founding contract for the first functional release (v0.1.0); none of them change the disk-truth state machine or the single-`status` worker contract. Numbering continues the stable reference. Build status: tracked per-row in §9 (as of v0.1.0 most are VERIFIED; the cadence/Windows floor matrix and FR-55 remain PENDING) — this section defines the target; no README or marketing claim derives from an FR until its §9 row is non-PENDING.*

**Scope decisions (2026-06-13).** The operator chose the full scope on all four open decisions, to be delivered in small, individually-tested increments (no deadline pressure; correctness over speed):
1. **In-context mode: in.** But the design separates the two things it was conflating — *dispatch-to-self* (a third dispatch mode, FR-46) and *cadence/wake-up style* (the existing rungs, FR-23). Motivation is dogfooding: Arunner runs continuously to orchestrate its own and QPB's development, which generates an always-on workload to exercise every cadence rung (own polling vs OS timer vs terminal-window ticker). Explicitly NOT a fix for the Class-C agent-loop drop (C-5/C-7); the dogfooding measures which wake-up modes survive real use.
2. **All five control commands: in** (PAUSE/RESUME, CADENCE, POOL, POLL-NOW, CANCEL), built in increments.
5. **Testing approach.** The release ships a deterministic integration-test suite, and Arunner dogfoods as its own test-run orchestrator (FR-51). FR-51 is the *foundational first increment* — the necessary-condition floor is built and pinned to current behavior before any FR-35..50 feature lands (a real regression test is the acceptance tests plus this floor, run together — see `SDLC.md`) — not a sub-item of the control commands. Dogfooding *measures* which wake-up modes survive real use; it never *validates* a §9 evidence row (those require a recorded matrix run, NFR-12).
3. **Both input adapters: in** (wrap + tail), with a one-field selector (FR-40/41).
4. **Full job-config UX: in** (`--check`, shorthand, examples, TOOLKIT builder, FR-42..44); maintaining two config formats is acceptable.

**Versioning**
- FR-34: **Single canonical version + startup banner.** Arunner carries one version string in one place (`arunner/__init__.py:__version__`, mirrored to `pyproject.toml` and `package.json`, drift-tested). Every surface reads it: the orchestrator's startup announce line, `tick.py --init`, the ticker startup, and the skill banner. A startup banner prints `arunner <version>` so the running version is always visible. Fixes the inconsistency carried in from extraction (the SKILL frontmatter `version: 1.5.9` was QPB's, while `pyproject` is `0.0.1`).

**Live control surface (the control-file convention)**
- FR-35: **Control-file convention.** Generalizing `STOP` (FR-10): the engine reads a fixed, closed set of named control files at the run-dir root — `STOP`, `PAUSE`, `RESUME`, `CADENCE`, `POOL`, `POLL-NOW`, `CANCEL` — at the top of each tick and applies them deterministically before transitions. **Reads and consumption happen inside the not-stopped path, so `STOP` keeps its fully-read-only tick (FR-10): if `STOP` is present it wins and nothing else is read or consumed.** When several controls coexist they apply in a fixed precedence — `STOP` > `CANCEL` > `PAUSE`/`RESUME` > `CADENCE`/`POOL` > `POLL-NOW` — which is the canonical evaluation order. Controls are one of two kinds: **one-shot** (`POLL-NOW`, `CANCEL`) are consumed (deleted) after they fire once; **sticky** (`PAUSE`/`RESUME`, `CADENCE`, `POOL`) set a persisted override in `harness_status.json` and govern subsequent ticks until changed (the file is consumed once its value is persisted). Control files work at every rung (agent, ticker, cron, manual), survive session restarts, and need no JSON editing. Unknown control files — and known control files carrying an unparseable or out-of-range value — are ignored with a warning row and never wedge the machine (Postel); consumption uses the engine's atomic write-temp-rename discipline so a STOP tick that finds a stray control still mutates nothing.
- FR-36: **PAUSE / RESUME.** A `PAUSE` file makes the orchestrator stop scheduling new ticks WITHOUT declaring the run `done` or `stop` (distinct from `STOP`, which halts terminally). In-flight workers keep running. Removing `PAUSE` (or dropping `RESUME`) resumes normal scheduling on the next tick. The status table shows a PAUSED banner while paused. When in-context mode is active (FR-46), `PAUSE` is observed at the next tick boundary — i.e. after the current in-context task finishes, since the turn is single-threaded (C-6) — and it suspends both background ticking and pulling the next instruction from the streaming queue (FR-47).
- FR-37: **Live cadence (and pool) override.** A `CADENCE <minutes>` control sets a persisted `tick_interval_minutes` override (sticky, per FR-35) that governs subsequent ticks without editing `plan.json`; a non-positive or unparseable value is rejected with a warning and the prior cadence retained (Postel). This reconciles the existing live-config asymmetry (today `tick_interval`/`stall`/`grace`/`idle_mult` are re-read from `plan.json` each tick while `pool_size` is sticky from `--init`): a `POOL <n>` control gives `pool_size` the same explicit runtime path. The two are not symmetric in mechanism — the cadence override layers over the per-tick plan re-read, while the pool override must write back to the sticky `harness_status.json` `pool_size`. Raising the pool back-fills dispatch on the next tick; lowering it below the current in-flight count is honored as slots drain and never kills a running worker.
- FR-38: **POLL-NOW.** A `POLL-NOW` control file forces exactly one immediate tick at the next opportunity — the unattended, file-based equivalent of the operator's "run another tick now"; consumed after one tick. `POLL-NOW` does not pierce `PAUSE` — while paused it is inert and waits for `RESUME` (per the FR-35 precedence).
- FR-39: **CANCEL.** A `CANCEL <run-NN>` control (run id in the file body or name suffix) marks a named run `abandoned` (terminal) AND writes a synthesized `abandoned` results record via the existing failure-synthesis path, so the run stays auditable in `results/` and in the SUMMARY (FR-45) rather than vanishing from the audit trail — freeing its pool slot, a deliberate operator out for the otherwise non-killable `stalled` state. `CANCEL` of an already-terminal or unknown run is a consumed no-op with a warning (it never un-terminals a completed run); an unparseable run id is ignored (Postel). (§8 keeps killing an in-flight worker out of scope; CANCEL frees the slot and stops watching the run; an orphaned worker, if any, runs to its own terminal — so the true running-process count may briefly exceed `pool_size` by design.)

**Flexible input (adapter at the edge — the engine contract is unchanged)**
- FR-40: **Wrap-and-run adapter (stdout capture).** A stdlib `heartbeat.py wrap` subcommand (the single worker-facing helper stays the adapter's home) launches an arbitrary command **as its own child**, redirecting the command's stdout+stderr to a file the adapter owns and tails. It emits `STARTING` immediately at launch, then `IN_PROGRESS` carrying the command's most recent output line as the ACTIVITY `label` — at least once within `launch_grace_minutes` and thereafter at ≤ ⅓ of `stall_threshold_minutes`, so a long-running quiet command never false-trips LAUNCH-FAIL or STALLED — and emits the terminal `COMPLETED`/`FAILED` directly from the command's exit code. Any command becomes a conformant arunner job with no change to the command itself. Because the adapter is the command's parent (not a third party watching a detached process), the capture is robust; the one caveat is display granularity — a command that block-buffers when its stdout is not a TTY may surface lines chunkily. Line-buffering is POSIX-best-effort only (a pty where available; `stdbuf` is absent on macOS/Windows and `pty` is POSIX-only), so on Windows output surfaces chunkily by design — a display-quality issue, never a correctness one, because **doneness comes from the exit code, never from parsing output.** v0.1.0 scope.
- FR-41: **Tail-existing-log adapter.** The adapter watches a log file a job already writes (pairing with FR-20's specifiable `heartbeat_path`), surfaces the most recent line as the ACTIVITY `label` in IN_PROGRESS heartbeats, and detects doneness via an operator-configured signal — a terminal-marker regex, a sentinel file the job touches, or process exit. This lets the harness monitor a job that emits *any* log format (not JSONL) while keeping the engine's single-`status` contract intact: **the adapter, not the engine, decides doneness** — the engine never guesses a terminal from arbitrary text. Process exit is the authoritative, always-available doneness signal; an operator-configured regex/sentinel is an optional overlay for jobs that signal completion only in their log — a success-marker maps to `COMPLETED`, an optional failure-marker to `FAILED`, default `COMPLETED` on a clean exit. The adapter records its supervised child PID in the run's claim lock so the engine's existing dead-PID reap backstops a sentinel that never arrives. FR-40 (wrap a command you launch) and FR-41 (tail a log a job already writes) are the two adapter modes; both are v0.1.0 scope. Use wrap when arunner owns the launch, tail when something else does. **A single plan field — `adapter: "wrap" | "tail"` (with the wrapped command or the tailed log path) — selects the mode, so the operator declares intent in one place and never wires the plumbing by hand.**

**Job-configuration UX**
- FR-42: **Plan pre-flight (`tick.py --check <plan>`).** Validate a plan before launch: schema conformance, presence of the required placeholders in each `worker_prompt`, existence of each `target_repo`, and (optionally) run each entry's `auth_check`. Report all problems at once. Schema conformance is a hand-rolled stdlib check against the plan schema's required keys (NFR-3 forbids a `jsonschema` dependency) and reuses the engine's placeholder set so the check can't drift from what dispatch actually substitutes. Catches config errors proactively instead of as a reactive `AUTH_OR_LAUNCH_FAILED` after launch spend.
- FR-43: **Simplified job shorthand + examples.** A higher-level `jobs:` plan form (e.g. `{repo, agent, prompt}` per job) that the skill/TOOLKIT expands into the full placeholder-laden plan, plus an `examples/` directory of ready-to-edit plan templates for the common cases (multi-repo agent review; shell jobs; mixed). The low-level plan schema stays canonical; the shorthand is a convenience layer.
- FR-44: **TOOLKIT as plan-builder.** `TOOLKIT.md` is the primary human-facing surface for creating configurations: the operator describes the batch in natural language and the agent writes a conformant `plan.json` (placeholders and defaults handled by the agent) per TOOLKIT's documented procedure.

**Result roll-up**
- FR-45: **SUMMARY artifact.** On the *transition into* `done` (guarded so a post-`done` idempotent re-tick stays cycle-only, FR-6), the engine writes both `SUMMARY.md` (human) and `summary.json` (machine, carrying `schema_version` per FR-2) to the run-dir: per-job terminal status, `result_file` pointers, durations, and the aggregate counts — a self-contained "what happened" capstone beyond the per-job `results/result-NNNNN.json` records.

**In-context worker mode (harness mode + in-context capability)**
- FR-46: **In-context capability (dispatch-to-self — a third dispatch mode, orthogonal to cadence).** Arunner separates two independent axes: *who does the work* (dispatch) and *what triggers the next check* (cadence, FR-23). In-context adds a third dispatch option alongside subagent (FR-14) and shell (FR-15): the orchestrator MAY do a task itself, in its own context, between ticks. It composes with any cadence rung for the dispatched/background portion of a run; the single cross-axis coupling is that the in-context *tasks themselves* need a live agent, so they ride on the agent's own scheduling (rung 1), while background workers the same run also watches can be driven by any rung. Harness mode (dispatch + monitor) is the base; in-context is a superset that retains every harness feature. Governing constraint: the agent turn is single-threaded, so in-context work and a harness tick are sequential, never simultaneous — while in-context work is in progress the agent cannot tick, so heartbeat reads, stall detection, dispatch, and status updates **pause** and resume between in-context tasks. Background processes/subagents keep running and heartbeating throughout; the next tick absorbs whatever changed (the engine already tolerates irregular ticks — FR-8's wall-clock-jump handling is the same property). In-context work takes attention-priority over the next tick; it does NOT preempt running background processes. An in-context session may freely spin off its own subagents/processes for sub-work (no capability limits); only concurrent monitoring is what pauses. In-context mode is enabled at bootstrap by an `instruction_folder` setting (a plan/bootstrap field); its presence selects the superset mode. **Limitation (C-7):** in-context mode does not fix Class-C loop drops and is rung-1 only — the deterministic floor (ticker/cron) rescues only the background/harness portion, never the in-context tasks, which require operator re-bootstrap (§8). It is a convenience/unification superset, not an unattended-reliability path (FR-50).
- FR-47: **Streaming instruction queue.** An in-context worker watches an instruction folder for tasks that arrive over time — a streaming queue, distinct from the plan's fixed batch (entries frozen at `--init`). Instructions are files named with a zero-padded numeric prefix (`NNN-...`); an instruction is "processed" when an output file carrying the same `NNN` stem exists in the outputs folder. It processes the lowest-numbered instruction with no matching output, writes that output, and idles when the queue is empty; a `STOP` file halts it (FR-10). This is the live-job-submission surface; the QPB runner is the degenerate case — instruction folder, no background plan.
- FR-48: **ERR discipline mandatory in-context.** Because in-context work fills the agent's context and is compaction-prone (unlike bounded-context harness mode, NFR-5), the in-context loop MUST externalize progress, recognize loss on resume, and rehydrate from disk (the externalize-recognize-rehydrate pattern). Arunner's disk state is the ERR substrate: on resume the agent re-reads `harness_status.json`, the instruction folder, and the outputs to re-establish exact state without trusting memory.
- FR-49: **Monitoring-pause visibility.** When the agent resumes ticking after an in-context burst, the status table notes the gap (e.g. "monitoring paused HH:MM–HH:MM for in-context work; N background changes since last poll") so the sparse cadence reads as intentional, not as a dropped loop.

**Testing**
- FR-51: **Deterministic integration-test suite (Arunner dogfoods itself).** The release ships an integration-test suite as a folder of scenarios — each a small plan plus stub workers plus an expected end-state — under `tests/integration/` (or equivalent). Two disciplines keep it trustworthy: (a) scenarios are driven by the deterministic ticker (`ticker.py --once` in a loop), never the agent loop, so a run is reproducible and CI-able and the flaky Class-C path never enters this deterministic floor; (b) the pass/fail verdict is an **independent** plain-Python check that reads the disk artifacts (`harness_status.json`, `results/`, heartbeat files) and asserts — the harness never grades its own homework. Because a scenario IS a arunner job, the same suite doubles as a worked example of Arunner orchestrating a batch (the dogfood). The suite is built first (before the FR-35..50 features) and pinned to current behavior, so every later increment runs against a standing necessary-condition floor (regressions surface when the acceptance tests run, not here — `SDLC.md`). This complements, not replaces, the deterministic-core unit tests (red/green where the logic is a pure function of disk state — which is most of it; the thin agent layer gets integration + dogfood coverage instead).

**Positioning**
- FR-50: **Unattended-reliability guidance.** README/TOOLKIT steer operators who need reliable *unattended* runs toward the deterministic ticker/cron rungs (immune to the Class-C agent-loop drop, C-5), framing the in-session agent rung as the interactive/convenient mode backed by the FR-26a safety tick. The agent rung is not presented as the unattended-reliability path.
- FR-54: **Cross-agent universality is the identity — lead with it, keep it honest.** The product orchestrates ANY agentic coding system — Claude Code, Copilot, Codex, Cursor, Antigravity, … — not one vendor. This is delivered by construction: stdlib-only Python (runs wherever Python 3 does), the vendor-neutral contract *a job is anything that appends JSON lines to a file*, and host-CLI/subagent dispatch (FR-14/15). Messaging (README/TOOLKIT/SKILL) leads with this, not with any single host. **Honesty split (load-bearing):** the deterministic engine and the ticker/cron floor are genuinely host-agnostic and run identically everywhere; the *in-session agent rung* is where host differences live (Class-C is a Claude Code quirk; other hosts have their own scheduling quirks), so the universal-*reliability* claim rests on the floor, with the per-host agent rung as the convenience on top. The README support table marks each host VERIFIED (evidence-linked — e.g. Copilot + Codex, macOS, rung 3, from V-14) vs DESIGNED (NFR-12); "runs on any agentic system" describes the engine + floor, never an unvalidated per-host agent-rung claim.

### v0.1.0 interactive UX (the build-and-run experience)

*Added 2026-06-13 after the build was feature-complete: the interactive session builder is what makes the tool usable and testable as a real user journey, and the cross-agent identity (FR-54) is its backbone. Numbering continues the stable reference; build status in §9 (FR-52/FR-53 now VERIFIED).*

- FR-52: **Interactive session builder (host-agent-driven; the tool stays LLM-free).** The headline UX: an operator describes a run in plain language to whatever agent has the skill loaded — "run three jobs from `ABC.md`, `DEF.md`, `GHI.md`, pool 2, subagents"; "run `abc.exe def.exe ghi.exe jkl.exe`, pool 3" — and a session is assembled, previewed, run, and optionally saved. **Architecture (load-bearing for FR-54):** the tool embeds NO model — the HOST agent does the natural-language understanding and drives the procedure documented in the SKILL/TOOLKIT; the tool provides only deterministic plumbing (expand the `jobs:` shorthand FR-43 → `--check` FR-42 → write the plan → run). The builder is *designed* to be driven by any capable host agent — but it runs in the host's natural-language layer, i.e. on the per-host **agent rung**, NOT the universal engine/floor (FR-54). So "any host agent" is DESIGNED; it is VERIFIED only on the host(s) that have actually driven it end-to-end (Claude Code today) per NFR-12 — V-14 verified Copilot/Codex as detached *workers*, not as builder-driving orchestrators. The procedure:
  1. **Describe → assemble.** The agent builds a `jobs:` shorthand, **inferring dispatch per job by *intent*, not file extension**, on this precedence: (i) an explicit operator override wins; (ii) anything readable as agent instructions (a `.md`/`.txt`/`.prompt` file, or an inline instruction) ⇒ **subagent** mode, with the file's content read into the worker prompt; (iii) anything resolving to a runnable command — with or without args — ⇒ **shell** mode, the full command-with-args wrapped via the FR-40 wrap adapter (`adapter: "wrap"`); (iv) otherwise **ask** a clarifying question, never guess. One `jobs:` list MAY mix subagent and shell entries. (Reading a file into a prompt and choosing the mode are host-agent behaviors — the tool stays model-free; a resolved-vs-ask disambiguation table lives in TOOLKIT/SKILL.)
  2. **Preview → confirm.** Show the assembled plan and the `--check` result, **echoing the inferred dispatch mode and prompt source per job** ("job 2: SHELL, wrapping `build.sh`") so a mis-inference is caught BEFORE spend, then wait for one explicit "go" — it spends real agent budget, so "just works" means low friction, not zero safety. If `--check` fails, no "go" is offered: the errors surface for editing (step 5). A confirmation is valid only for the exact previewed plan.
  3. **Run.**
  4. **Persist on request.** "Save that to `my_run.json`" writes ONE file carrying both a `jobs:` key (the human-editable shorthand source) and a `plan:` key (the expanded canonical entries — the reproducible lockfile). `run my_run.json` executes the `plan:` (lockfile semantics); on load the tool **re-expands `jobs:` and warns — does not block — if the result differs from the saved `plan:`** (a hand-edit-drift signal), offering to re-expand or run the lockfile as-is. (Writing the expanded plan to a path is a small new affordance — `expand --out <path>` — since the expander today only prints to stdout; the save path is absolute, never cwd-derived (FR-21a), and overwriting an existing file is confirmed.)
  5. **Incremental editing.** Before launch — including when re-opening a saved-but-not-running plan — the conversation refines the run: "add a fourth job", "change pool to 3", "make job 2 shell". **Any edit returns the session to the unconfirmed state** (re-preview + re-`--check` + a fresh "go"). Editing a *live* run is out of scope here — that is the live-add path (FR-57 for the harness pool batch, or FR-47's streaming instruction folder for an in-context worker).

  **Scope:** the builder AUTHORS-AND-LAUNCHES a fixed batch; dropping new jobs into a *live* run is the live-add path (FR-57 / FR-47), not the builder.
- FR-53: **First-class lifecycle verbs.** A coherent CLI vocabulary for the whole run lifecycle, so "start it, watch it, stop it, rerun it" is one obvious surface — and the backbone of an end-to-end integration test of the real user journey: `new` (interactive build, FR-52), `run <plan>` (launch an authored/saved plan), `status <run-dir>` (re-attach: read disk and print the current status table, read-only), `stop <run-dir>` (drop the `STOP` control file, FR-10, to halt cleanly), `resume <run-dir>` (continue an interrupted run; by default runs the ticker loop at rung 3 against the existing run-dir — the named front-end for the FR-13 resume — with `--once` as the single-tick form; idempotent), `summary <run-dir>` (print the SUMMARY capstone, FR-45; on a not-yet-done run it prints a "not done yet" notice rather than erroring). All are thin deterministic wrappers over existing capabilities (read disk, format the table, write a control file, tick, read SUMMARY) — no new engine state. Finer live control (pause/cadence/pool/cancel) stays the control-file convention (FR-35..39), not separate verbs. (The command name follows the final product name.)

### v0.1.0 autonomy-integrity addition

*Added 2026-06-14 after a live incident: the orchestrator stopped an in-flight build overnight on its own judgment ("it's late for the operator"), the literal opposite of the unattended autonomy the product exists to provide. FR-55 makes that decision external and auditable: the engine computes it, the host may not silently substitute its own judgment, and any instance where it does becomes a recorded artifact (caught post-hoc, not prevented). Numbering continues the stable reference; build status PENDING (§9).*

- FR-55: **Continuation contract (autonomy integrity — the halt decision is external and audited).** Unattended autonomy is Arunner's core promise. An LLM host has a standing turn-completion bias that surfaces as plausible-sounding reasons to stop (a late hour, a tidy checkpoint, "checking in"); the same bias inverted is continuation pressure. Both are the agent's judgment overriding the operator's actual control state. FR-55 does not try to legislate the bias away — it makes the continue/stop decision external (engine-computed) and the host's relinquishments auditable, so every instance of the bias becomes a recorded artifact instead of an invisible judgment:
  - **Continuation verdict (engine, deterministic).** Every tick ends by computing a verdict that is a pure function of run-dir state: `CONTINUE` iff the run is non-terminal ∧ the persisted status is not paused/stopped/cancelled ∧ no open blocker is recorded ∧ the run can still make progress; otherwise `HALT:<reason>` with `reason` in the **closed set** `{done, failed, stop, pause, cancel, blocked:<id>, stalled, budget, internal_error}`. The members: `done` (all entries terminal and clean); `failed` (any non-clean terminal, including `auth_or_launch_failed`); `stop`/`pause`/`cancel` (operator control states, FR-10/35–39); `blocked:<id>` (an operator-decision gate — see below); `stalled` (the non-terminal, non-killable wedge `tick.py` marks on heartbeat-age overflow with nothing dispatchable — the operator's out is `CANCEL`); `budget` (an operator-set `--max-ticks`/iteration budget exhausted, e.g. the ticker's `--max-ticks` exit); `internal_error` (an engine fault — corrupt status, unwritable run-dir — the catch-all that keeps the closure claim true). **The verdict reads the persisted status** (`status.paused`, terminal states, the blocker record), **not raw control-file presence**, because FR-35 consumes control files once persisted (a consumed PAUSE leaves `paused: true` but no file). The verdict carries `next_tick_due` (derived from the engine's existing `next_tick_minutes`, FR-5) and a `monitoring_paused` flag (set during an FR-46 in-context burst, where ticking is suspended rather than scheduled). It is written each tick to `harness_status.json` and the journal so an independent reader can audit it. (This formalizes, as an explicit emitted value, the continue/stop decision the tick loop already makes implicitly via STOP-read-only gating (FR-10/35) and terminal-state detection.)
  - **Orchestrator contract.** While the verdict is `CONTINUE` **and a tick is due** (rung 1: immediately; rungs 2/3/4: when wall-clock ≥ `next_tick_due`), the host MUST advance another tick; it MAY relinquish control to the operator only on `HALT`. A *scheduled* wait between ticks (the cadence rungs FR-23, the FR-26a safety-tick interval) and an in-context pause (`monitoring_paused`, FR-46/49) are neither a yield nor abandonment — `CONTINUE` means "the run is not finished," not "tick again this instant." Time of day, a "natural stopping point," "checking in," effort/context conservation, and turn-completion are **named non-reasons** for relinquishing while a tick is due. A genuine operator-decision point counts only if it is recorded as a `blocked:<id>` blocker (below); narrating a blocker in chat and then stopping without recording it is itself a violation.
  - **Blocker records (`blocked:<id>`).** The one new state. A blocker is a disk record (a `blockers/` entry at the run-dir root, or a `blockers` array in `harness_status.json`) carrying `id`, `created_at`, `reason`, `cleared_at`; while any blocker is open the verdict is `HALT:blocked`. The operator clears it (a `clear-blocker`/RESUME-style action the engine reads), which flips the verdict back to `CONTINUE` and resumes via FR-13. It differs from `CANCEL` (FR-39), which *terminates* a stuck run (`abandoned`): a blocker *suspends* a healthy run awaiting an operator decision, then resumes. **Known limitation:** the blocker is host-authored, so it is a residual discretion surface the detector cannot fully close — a biased host could fabricate a blocker to manufacture a clean yield. The verdict cross-check (below) catches a yield that *claims* a verdict the engine didn't emit, but it cannot adjudicate whether a recorded blocker was *genuinely* necessary; that residue is acknowledged, not eliminated.
  - **Yield accounting + detector.** When the host relinquishes control it appends a `yield` record to the journal naming the verdict it observed. **Three** violations are detectable from disk alone: (i) **silent abandonment** — verdict `CONTINUE`, wall-clock past `next_tick_due` by more than a tolerance margin (≥ one cadence interval, to absorb FR-8 wall-clock jumps and scheduler jitter; the clock is suspended while `monitoring_paused`), the run never advances again (**terminal non-resumption** — a crash later recovered by FR-13 resume is *not* abandonment), and no yield record; (ii) **illegitimate yield** — a yield citing a reason outside the closed halt set while the verdict was `CONTINUE`; (iii) **false halt claim** — a yield whose *claimed* verdict does not match the engine's *actual* recorded verdict for that tick (e.g. cites `HALT:done` while the tick verdict was `CONTINUE`). The checker compares each yield's cited verdict against the engine's on-disk verdict for the same tick, so the yield's honesty is *verified against ground truth, never trusted* — an in-set fabricated reason (the most plausible-sounding, most likely real-world stop: "we're basically done") is caught exactly like an out-of-set one. **Honesty of the mechanism:** the engine cannot force the host to read the verdict, and the detector does not depend on its having read it — ignore-the-verdict-and-stop produces the same `CONTINUE`-state-without-valid-yield artifact the detector flags. The failure is caught **post-hoc, not prevented**: the overnight stop can still happen; FR-55 converts it from an invisible judgment into a recorded violation the FR-51 independent checker flags — in both the deterministic stub-host scenario (`continuation_contract`) and a live dogfood audit (NFR-12 measurement). You cannot legislate the discretion out of an LLM; you make every instance of it produce an auditable artifact.

### Adapter addition (activity-pattern extraction)

*Added 2026-06-14, revised after a 3-panel council. The wrap/tail adapters surface the most-recent raw output line as the ACTIVITY label, which is noise when the wrapped tool is chatty. FR-56 lets the operator declare which lines are activity-relevant. **Built** — display-only activity-pattern extraction in both adapters, with `--check` validation + reduced-and-disclosed ReDoS bounds; the cross-platform simulator's noisy-log mode was the test substrate. VERIFIED (§9).*

- FR-56: **Activity-pattern extraction for the wrap/tail adapters (operator-declared relevance).** Today both adapters (FR-40 wrap, FR-41 tail) display the *most-recent non-empty output line* as the worker's ACTIVITY label (`heartbeat.py` `_last_output_line` for wrap; the tail's last line). For a chatty tool that is noise. FR-56 lets the operator declare a set of **activity patterns** (regular expressions); the adapter shows, as the ACTIVITY label, the most-recent output/log line that **matches an activity pattern** — filtering noise to the lines the operator deems relevant. This is what lets arunner surface meaningful status from a tool that knows nothing about arunner (FR-54). *(Named `activity`, not `status`, deliberately: `status` is the one field the harness interprets (FR-18); this only affects the displayed ACTIVITY column.)*
  - **Scope: display/activity only — NOT doneness.** FR-56 governs the human-facing ACTIVITY column. It does NOT change lifecycle/terminal determination: wrap doneness stays exit-code-only (FR-40); tail doneness stays the FR-41 precedence (success/failure-regex overlay → process exit). A pattern that never matches degrades the *display* (it falls back to a neutral label), never the *correctness* of done/failed. **Boundary (FR-18):** the adapter is a worker-side heartbeat *producer* selecting a `label`; the engine *reader* still interprets only `status` — FR-56 does not make the harness interpret output, it only changes how a worker-side helper chooses its display label (the same latitude FR-40/41 already exercise).
  - **Matching semantics + staleness honesty.** Patterns are applied to each new output/log line **within the per-tick scan window** (tail rides the existing incremental `_LogTail.new_lines()` pass it already uses for doneness regexes; **wrap needs a new incremental reader** — its current `_last_output_line` reads the whole capture file and returns only the last line, so "most-recent *matching* line" is genuinely new state in `_Keepalive`, not a free ride on an existing pass). A line is *relevant* if it matches any pattern; the most-recent relevant line in the window becomes the label. **Because a matched line pins indefinitely once the tool goes quiet — which would look live but be stale, worse than the raw last line that at least keeps moving — the label carries an age hint when its source line is older than the current keepalive** (e.g. `Step 7/12 (8m ago)`). If no line has ever matched, the label is the neutral "(running…)" placeholder. *(A capture-group option — display just a matched substring rather than the whole line — is deferred; v1 shows the whole matched line, truncated.)*
  - **Config surface (both adapters).** Per-job `adapter_activity_patterns: ["...", "..."]`, synthesized by the engine into repeated `--activity-regex` args **for both wrap and tail** (today the wrap branch of `_adapter_worker_cmd` carries no optional flags — FR-56 adds them there too), consistent with how FR-41's `--success-regex`/`--failure-regex` are synthesized. Cap: **≤ 16 patterns** per job.
  - **`--check` validation (FR-42).** Pre-flight (`_check_adapter_entry`) rejects: a non-list; a non-string or **empty** element (`re.compile("")` matches every line and silently defeats the filter); more than the cap; a pattern that fails `re.compile` (naming the pattern + `re.error`); and a pattern failing the complexity screen (below). *(Retrofit: the existing success/failure regexes are currently only type-checked, not compiled — FR-56 compiles all adapter regexes at `--check` for consistency.)*
  - **Safety (ReDoS) — reduced and disclosed, not eliminated.** Operator regexes run against arbitrary unbounded external output, and stdlib `re` has no match timeout (the `regex` module that does is barred by NFR-3). Honest accounting: per-line truncation caps *input length* but does **not** bound catastrophic backtracking, which is a property of the *pattern* (operator-controlled). Mitigations, all stdlib: (a) truncate each line to a bounded length (≈4 KiB) before matching; (b) a **per-tick total-bytes-scanned ceiling** (≈256 KiB) after which activity-matching stops for that tick and the last label is retained — this is the bound that actually protects the tick loop from a chatty tool × N patterns; (c) a **compile-time complexity screen** at `--check` — a pattern-length cap plus a conservative heuristic rejecting nested unbounded quantifiers (scan the `re` parse AST for a `MAX_REPEAT` directly inside a `MAX_REPEAT`), documented as a heuristic; (d) the ≤16-pattern cap. Even so, a determined operator can wedge a single tick with a pathological pattern: this is an **operator-trusts-own-config** surface, the same NFR-11 trust bucket as `worker_cmd`/`command`, and the residual risk is stated, not hidden. *(Implementation note: the engine's `{TASK_ID}`-style template substitution must not rewrite a token-shaped substring inside a pattern — a documented footgun, not injection, since patterns are passed as argv elements, never through a shell.)*
  - **Cross-platform / determinism.** Pure stdlib `re` over the incremental line readers; no new dependency (NFR-3), cross-platform (NFR-1), unit-testable by feeding fixed lines + clock values to the matcher (the adapters already isolate `new_lines()` / `_last_output_line` and the `_Keepalive` clock seam for exactly this).

### Live-ops additions (live enqueue + live monitoring cadence)

*Added 2026-06-15 from the first real stub-worker pressure test (a 30-job / pool-4 run worked cleanly, but growing a running batch meant hand-editing the run-dir, and the ACTIVITY column sat at STARTING with no visibly-streaming table). Revised after a 3-panel council. FR-59 added the same day from the first real QPB subagent run, where a long synchronous subagent worker blocked the orchestrator's turn so the table couldn't refresh — surfacing that the externalized-state design already makes live display a separate read-only concern. PENDING (§9).*

- FR-57: **Live enqueue (`add`) — stage-and-absorb.** Grow a *running* harness batch's queue without re-`--init`. A CLI verb — `arunner add <run-dir> <plan-or-jobs-file>` (or `… --command … --pool …`) — writes the new, validated entries to a **staging area in the run-dir (`incoming/`)**; the **next tick absorbs them under the lock it already holds**, appending to `plan.json["entries"]`, scaffolding each new `run-NN` (heartbeat, manifest, claim token) and a `queued` record in `harness_status.json["runs"]`, mirroring `--init`'s `init_run`. This stage-and-absorb model is **race-free by construction** — it matches the engine's "next tick absorbs whatever changed" idiom; `add` never mutates the live files a concurrent tick reads/writes. (The engine rebuilds the work table *positionally* from `plan.json`, so "append to the `queue/` folder" is a category error — those are claim tokens, not a work list.) Disciplines:
  - **Append-only numbering.** New `run-NN`/`task_id` number append-only from the current entry count; existing runs are never renumbered (a renumber or a `len(entries)`↔`runs` mismatch silently drops the job through the tick's swallowed `except`).
  - **Placeholders unresolved.** Stored entries keep `{TARGET_REPO}`/`{HEARTBEAT_PATH}`/… **unresolved** — dispatch-time substitution (FR-21a) handles them; pre-resolving re-introduces the transcription hazard.
  - **`--check` is a hard pre-land gate** (FR-42): the add-input is validated before it touches the run-dir; a bad entry never lands. `task_id` minted if absent.
  - **Re-activation is automatic.** `done` is recomputed each tick, so a new `queued` entry re-activates an idle (done) run on the next tick — `add` writes **no** `done` field; the queued entry is the activator. (Adding to an already-`done` run is in scope; harness *iterate* strategies stay deferred, §8.)
  - **Distinct from FR-47:** FR-47 streams *instructions* to an in-context worker (instruction folder, `NNN-` files, rung-1, no pool); FR-57 grows the *harness pool batch* (engine claims/dispatches/reaps `run-NN`). The live-add path is now FR-47 *or* FR-57 (FR-52's scope note is updated accordingly). Extends **UC-1** (run a batch → grow it; new alt-path). Append-only is the minimal scope — remove/edit are covered by FR-39 CANCEL / FR-52 pre-launch editing.

- FR-58a: **Frequent activity-refresh cadence (engine).** The wrap/tail keepalive interval is `min(launch_grace, stall/3)` ≈ 10 min at defaults, so a short job emits only STARTING + terminal and the FR-56 activity patterns never fire; even long jobs refresh only every ~10–15 min. Add a **configurable keepalive/activity-refresh interval** — `--keepalive-seconds` + a plan field, **default ~45 s** — that drives the IN_PROGRESS keepalive (which already re-scans the activity patterns in the *same* `maybe_emit` event — do NOT split re-scan from emit), **decoupling the *interval* from the `stall/3` arithmetic** (the stall threshold stays coarse for genuine stall detection; timer-driven keepalives never mask a hung-but-alive child — the dead-PID reap does that). Required:
  - **First scan at start:** emit one activity-scanned IN_PROGRESS immediately after STARTING, so a job shorter than the interval still surfaces a line.
  - **Synthesize the knob in `_adapter_worker_cmd`** — and fix the latent bug that `--launch-grace-minutes`/`--stall-threshold-minutes` are *never* synthesized into the adapter today (they fall back to the hardcoded 10/45, so the plan's grace/stall are inert for adapter jobs).
  - **`--check` rejects `keepalive_seconds > launch_grace_minutes·60`** (fail-loud, FR-42); explicit-override-wins; 1 s floor (Postel posture).
  - Cheap (NFR-13: one sub-200-byte append per ping); the FR-56 256 KiB ceiling is *per scan over the new-lines delta*, so finer scanning makes each delta **smaller**, not larger.
  - **Acceptance test drives the DEFAULT keepalive path, not `grace 0`** — the FR-56 EndToEnd tests used grace 0 (floored to 1 s), which is exactly why this default-path breakage shipped unnoticed. Extends **UC-2** (monitor; new alt-path: ACTIVITY moves during a run).

- FR-58b: **Regular, visible table (orchestrator + SKILL — hardening FR-27/UC-2, not new ground).** The engine already emits `status_table` every tick (FR-5/FR-27); the operator's miss was *behavioral* — the rung-1 agent ran the ticker in one collapsed bash block, so the per-tick tables never surfaced. This is **~100% SKILL/runbook, ~0% engine**: the SKILL/runbook states the rung-1 orchestrator MUST print `status_table` **each tick as a visible message** (not captured/suppressed, not buried in one long ticker block). It is a per-host **agent-rung** concern (DESIGNED, NFR-12), graded separately from FR-58a's engine change — a green FR-58a engine test does NOT verify FR-58b. (Optional engine backstop: a delimited per-`--once`-tick table print in `ticker.py`, only if scoped with its own test.) Note FR-58a is inert for operator *visibility* without FR-58b, and ACTIVITY still only moves as often as the tick that renders it.

- FR-59: **Read-only disk monitor (`arunner monitor`) — display decoupled from orchestration.** A standalone, **strictly read-only** process that renders the live status table by polling a run-dir's externalized state, so monitoring never depends on the orchestrator getting a turn. Because all run state is on disk (`harness_status.json`, the per-run heartbeats, `plan.json`), the display is just another consumer (NFR-9): `arunner monitor <run-dir> [--interval SECONDS] [--once]` reloads the run state and re-renders every interval (default ~2 s). This directly closes the gap **C-6** opens: while a long **synchronous subagent** worker (e.g. a QPB run) blocks the orchestrator's turn so no tick fires, the engine's `harness_status.json` is frozen at the last tick — **but the workers' own heartbeat files keep updating live** — so a sidecar monitor surfaces that progress without waiting for the orchestrator to unblock. Disciplines:
  - **Strictly read-only — the load-bearing safety property.** The monitor NEVER writes any file, NEVER creates or acquires `.tick.lock`, NEVER drops a control file, NEVER advances a tick. It is safe to run concurrently alongside a live engine on **any rung** (in-agent, ticker, cron, manual) precisely because it cannot race the writer. This is the single property the whole feature rests on; it is the thing the tests must pin hardest.
  - **Shared renderer, no fork.** It reuses the engine's existing `_format_table(run_dir, status, plan, terminal)` (FR-5/FR-27) rather than duplicating it, so the monitor and the engine can never drift into two table formats. That renderer already reads each run's heartbeat *live at render time* (`_hb_observe`/`_hb_age_str`) — so ACTIVITY and HB-AGE refresh every poll for free, while the run *lifecycle* states refresh only when the engine rewrites `harness_status.json` each tick.
  - **Freshness honesty (NFR-12).** The monitor surfaces the **age of `harness_status.json`** ("state as of last tick: Xs ago") so the fast display refresh never implies the run-state data is fresher than the last engine tick. It advances nothing and makes nothing fresher — it re-renders existing disk state. The fast-moving columns (ACTIVITY/HB-AGE) are exactly as fresh as the heartbeat files; the lifecycle columns are as fresh as the last tick.
  - **Cross-platform, stdlib, cheap (NFR-1/-3/-13).** Clear-and-reprint each interval, stdlib only, no `curses` (Windows-weak), **no shell-out** (NFR-11 — not `os.system('clear')`; use an ANSI clear with a `--no-clear` append fallback for dumb terminals). Tolerates a momentarily-absent/locked/partway file (the engine's atomic write-temp-rename means a successful read is never partial; a transient read failure skips a frame, never crashes). `--once` prints one snapshot and exits; the loop prints a final render and exits 0 when the run reaches a terminal state (done/stop), or on Ctrl-C.
  - **Not a substitute for FR-58a/58b.** The monitor decouples *display refresh from orchestrator turn-blocking*; it does not change how often the engine ticks or how often ACTIVITY data is produced. It complements FR-58a (data cadence) and FR-58b (in-session table print): the in-session table is the orchestrator's own witness; the monitor is the rung-independent sidecar for when the orchestrator is busy or absent. Extends **UC-2** (monitor; new alt-path: rung-independent read-only sidecar).

- FR-60: **Chat ⇄ runner message channel (typed inbox/outbox, acknowledged).** A typed, bidirectional, acknowledged control channel so an orchestrating chat can drive a *running* session and get structured results back — without restarting the runner. Generalizes FR-57's stage-and-absorb from "enqueue jobs" to "arbitrary control messages." The chat drops a message file in `<run-dir>/inbox/<ulid>.json`; the engine **drains the inbox at the start of each tick under the `.tick.lock` it already holds**, processes each message **idempotently** (processed-ids ledger → crash-safe replay), writes an **acknowledgement** to `<run-dir>/outbox/<ulid>.ack.json` (`received`→`applied`/`rejected`+reason), and a **result** payload to `<run-dir>/outbox/<ulid>.result.json` when the triggered work completes. Closed verb set (NFR-4): `enqueue` (= FR-57 `add`), `control` (= FR-35..39 pause/resume/cadence/poll-now/cancel), `dispatch-job` (enqueue one subagent job with a given `worker_prompt`), `run-batch` (a named pre-registered batch), `snapshot` (emit current `status_table`/counts to the outbox — read-only), `note` (journal an audit note). Disciplines mirror FR-57: drain-under-lock, idempotent-by-id, append-only outbox, `--check` validates every inbound message (malformed → `rejected` ack, never crashes the tick), read-only verbs mutate no run state (never-writes pin), placeholders stored unresolved, **no network listener** (local-disk inbox only; NFR-11) — the engine executes only the closed verb set and passes `worker_prompt`s to subagents as data, never shell-evaluated. **Higher-level loop semantics live in the chat and the job prompts, not the engine** — the engine stays a generic, deterministic message→dispatch relay. Adds **UC-13** (chat-driven generation: run-batch → snapshot → dispatch-job → read outbox → decide → next, all without restarting the tick loop). *Born 2026-06-16 from the autonomous-security-improvement-loop design (`AI-Driven Development/Security Improvement Loop/03_Arunner_Message_Inbox_Spec.md`), which needs a chat to drive conditional, iterative generations.*

### Multi-step / gated-pipeline / token-reporting additions (arunner 0.2.0)

*Added 2026-06-18. These five requirements (FR-61..65) target arunner **0.2.0** — the engine ships 0.1.0 today, and the 0.2.0 version bumps when their implementation lands, not in this spec change. They extend the founding contract for multi-step, gated pipelines and token reporting **without touching the disk-truth tick model (FR-4/6), the single-`status` worker contract (FR-18), the "harness never grades its own homework" discipline (FR-51), or determinism (NFR-6)**. Step state is just more disk; gates are deterministic-first (the one optional AI decision is fenced to a separate judging context); tokens are reporting-only and never interpreted for control flow (NFR-12 measured-vs-unverified honesty holds). Motivating downstream consumer: QPB's multi-phase playbook, replacing its `run_playbook.py` orchestration. Council-reviewed 2026-06-18 (3-panel, SHIP) with seven FIX-REQUIRED items integrated. Build status PENDING (§9) — implementation lands in later instructions.*

- FR-61: **Prompt-from-file with light `{var}` templating.** An entry or step MAY supply `worker_prompt_file` (a path to a Markdown/text prompt) instead of an inline `worker_prompt` (FR-2); **exactly one of the two** is present (both = `--check` error, FR-42). Inline stays the default. This relaxes `plan.schema.json`'s previously-unconditional `worker_prompt` requirement into an exactly-one-of `oneOf` over {`worker_prompt`, `worker_prompt_file`, `steps`, `adapter`}, mirroring `heartbeat.schema.json`'s `oneOf` — a deliberate **constraint relaxation**, not a mere additive field. The path resolves relative to the **plan file's directory** (an absolute path is taken as-is), and its content is **snapshotted into the run-dir at `--init`** so the run is a self-sufficient record (NFR-9) — mutating the source file after `--init` never changes the dispatched prompt. A flat plan-level or entry-level **`vars` map** substitutes operator-declared keys by a **designated replace-key pass** (a literal `str.replace` of each `{key}`), run **before** the engine's reserved placeholders (`{HEARTBEAT_PATH}`/`{TASK_ID}`/`{RUN_DIR}`/`{TARGET_REPO}`/`{HARNESS_BIN}`, FR-3/21a). The replace-key pass substitutes **only the declared keys** and does **not** scan for or reject stray braces, so a prompt may carry literal single-brace JSON blocks (e.g. QPB's phase3/phase5 prompts) that survive substitution unmangled and never trip an unresolved-`{name}` error. A `vars` value may not itself contain a reserved engine token, and a reserved placeholder name is not a valid `vars` key (both `--check` errors), so `vars` can never spoof a dispatch-time path. Motivating case: QPB's `{skill_fallback_guide}` injection.
  *Acceptance pins:* a file-prompt dispatches byte-for-byte after substitution; the inline path is unchanged (regression); plan-dir-relative resolution works from a non-cwd working directory; the snapshot is authoritative (mutate the source after `--init` → run unaffected); the `{skill_fallback_guide}` case round-trips; **a prompt file containing a literal single-brace JSON block survives substitution unmangled and does NOT trip the unresolved-`{name}` error**; `worker_prompt`+`worker_prompt_file` both present → `--check` error; a `vars` key colliding with a reserved name, or a `vars` value containing a reserved token → `--check` error.

- FR-62: **Multi-step entries (ordered sub-runs).** An entry MAY declare `steps: [...]` — each step its own prompt (`worker_prompt` or `worker_prompt_file`) plus optional per-step `target_repo`/`worker_cmd`/`adapter`/`vars`/`gate` — **instead of** a single entry-level prompt (either/or; an entry carrying both a top-level prompt and `steps` = `--check` error). Steps run **sequentially within the entry, occupying one pool slot** for the whole sequence (the entry, not the step, is the unit of concurrency). On disk each step is a full FR-18 sub-run at `run-NN/steps/step-MM/` with its own `manifest.json`, `heartbeat.ndjson`, and result; the run record carries `step_index` and `step_count`. The per-step manifest reuses `job_manifest.schema.json`, whose `dispatch_mode` enum is **widened to include `shell`** so shell/adapter steps are representable (a pre-existing gap FR-62 makes reachable). The engine **dispatches only the current step per tick**; a step advances to its successor only on the predecessor's clean terminal (modulo a FR-63 gate). **Resume** reads `step_index` and continues — already-terminal steps are reaped, **never re-run** (NFR-6), an in-flight step is re-detected (FR-8/15), and a passed gate verdict is read from disk, not recomputed. The monitor/TUI (FR-27/59) show `step N of M` plus the step `label`; SUMMARY (FR-45) lists per-step statuses.
  *Acceptance pins:* a 3-step entry runs in order; with `pool_size: 1` and two multi-step entries the second waits for the first to finish all its steps; `step_index`/`step_count` appear on disk and in the status table; resume at step 2 dispatches step 3 with step 2 reaped-not-re-run (mutation-verified); a crash between reaping step K and dispatching step K+1 resumes correctly; a failed step drives the entry to a terminal state recording the per-step failure; `--check` rejects an entry carrying both a top-level prompt and `steps`.

- FR-63: **Continuation gates between steps (deterministic default; reasoning fenced).** Between step K and step K+1 a step MAY declare a `gate`, evaluated after K reaps clean and before K+1 dispatches, recorded as `step-MM/gate.json` and **read on resume, never recomputed** (NFR-6). Two kinds:
  - **`kind: "shell"` — the DEFAULT, and the only kind allowed in measurement/benchmark runs.** An operator-declared `argv` (NFR-11 trust bucket; placeholders and `vars` substituted by the engine) is run and its **exit code maps to a gate outcome (FR-64) — exit-code only.** There is **no** stdout/regex interpretation: a stdout-to-control-flow path would reintroduce the "engine parses text" hazard (FR-40/41/56) and add a ReDoS surface, so it is excluded by construction. A shell gate is a pure function of disk plus exit code — fully generic, testable, no LLM. *(Reference gate for QPB's per-phase preconditions, e.g. a file-existence check: a trivial `test -f <path>` argv whose exit code gates the next phase — the concrete plan-authoring deliverable that ports QPB's preconditions.)*
  - **`kind: "reasoning"` — OPTIONAL, FENCED, off by default.** Three mechanically-enforced rules: (1) **separate judging context** — the judge is a distinct worker/subagent from the judged step, the judged step's artifacts are passed as data, and the judge identity is recorded; a same-context judge is a `--check` error (upholds FR-51, "never grades its own homework"); (2) **structured logged verdict** — a closed-set JSON verdict carried via the FR-18 `data` channel; a malformed or absent verdict → `internal_error` and a fail-closed halt; (3) **excludable** — a reasoning gate requires plan-level `allow_reasoning_gates: true` plus the per-gate `kind`, and is **rejected at `--check` in any `measurement: true` run, any FR-51 harness run, or any FR-55-audited run**. The verdict is **computed at most once, persisted, and never recomputed** on resume (NFR-6 holds).
  *Acceptance pins:* each shell-gate exit-code outcome drives sequencing and is recorded/read-on-resume; a reasoning gate is rejected at `--check` without the opt-in OR inside a measurement run (mutation-verified); a separate-context judge emits a structured verdict with its identity recorded, and a same-context judge is a `--check` error; a malformed verdict halts fail-closed; the deterministic path is graded by the FR-51 independent checker (no LLM in the grader).

- FR-64: **Gate outcome vocabulary (closed set).** A gate resolves to exactly one of a closed set, each recorded verbatim in `step-MM/gate.json`:
  - `continue` — dispatch the next step.
  - `halt` — terminate the entry; it **maps to an existing FR-55 closed-set verdict** (terminal `failed`, or `blocked:<id>` when the halt is a recoverable blocker), never a new ad-hoc terminal, so the FR-55 auditor and FR-51 checker can classify a gate halt straight from disk.
  - `skip-to-next` — a **generic branch**: skip the immediately-following step, or `skip_to: step-MM` to skip forward to a named step; each skipped step gets a synthesized, auditable `skipped` terminal. *(Non-QPB example: a build pipeline whose lint step a shell gate skips when no source files changed.)*
  - `behavior-flag:<name>` — **continue AND set a named recorded flag** the next step's prompt consumes as a `vars` entry; `<name>` is **operator/plan-declared, never judge-chosen**. This is how QPB's Phase-3-skip is modeled: in `run_playbook.py` the Phase-3 skip is **worker-decided** (the worker writes a `_phase3_skipped_sentinel`; downstream steps only *read* it to suppress a warning) and **Phase 4 always runs** — a `behavior-flag` exactly, not a gate-driven `skip-to-next`.
  - `internal_error` — fail-closed → halt (the malformed/absent-verdict and out-of-set cases land here).
  The set is closed; any out-of-set value → `internal_error`.
  *Acceptance pins:* each outcome's effect is recorded; `behavior-flag` exposes the flag as a next-step `vars` entry and records it; the QPB Phase-3-skip pin asserts the **sentinel-write-by-worker / sentinel-read-by-downstream** mechanism (Phase 4 still runs), not a gate-driven skip; an out-of-set value → `internal_error` halt (mutation-verified); the independent checker discriminates halt/skip/flag from disk.

- FR-65: **Per-run and per-sub-run token reporting (input + output).** arunner reports input/output token counts per run and per step, **displayed and rolled up but never interpreted for control flow** (FR-18 untouched). A worker carries counts as **`data.usage = {input_tokens, output_tokens}`** on its heartbeats; the engine reads **exactly `data.usage`** (and nothing else in `data`, which stays opaque) into the result record at reap. The result record gains **top-level optional `input_tokens`/`output_tokens` properties — siblings of `summary`/`synthesized`, not nested under `claimed`** — and the engine sums them to per-entry and per-run totals. Counts surface via a **TOKENS** column in the status table (FR-27) and monitor (FR-59) and in SUMMARY (FR-45 `SUMMARY.md` + `summary.json`). **Graceful degradation (NFR-12):** a run with no usage shows `—` (never a fabricated `0` or estimate); a partial run shows real counts where present and `—` where absent, with the aggregate labeled `partial (3 of 5 jobs reported)`; a malformed `data.usage` is skipped-with-warning, never fatal. (QPB's `run_playbook.py` does not emit `data.usage` today, so its TOKENS render `—` until it is extended — graceful by design.) Tokens **never** change the FR-5 `{done, stop}` outcome.
  *Acceptance pins:* `data.usage` lands in the result record and rolls up additively (mutation-verified sum); per-step counts plus the entry total equal the sum; the table, monitor, and SUMMARY carry the counts; a no-usage run shows `—` and an unreported aggregate (never `0`/estimate); a partial run is labeled `partial (N of M jobs reported)`; a malformed `data.usage` is skipped-with-warning; tokens never alter the `{done, stop}` outcome.

### Single mode-discriminated plan format (FR-66..69, arunner 0.2.0)

*Added 2026-06-20 (instr 004). A clean-sheet collapse of the two-dialect plan format (`entries:` canonical + `jobs:` shorthand, with `id`/`task_id` + `repo`/`target_repo` synonyms) into ONE format: a single `jobs` list with friendly keys and a single `mode` discriminator. Per the SETTLED DIRECTION in `docs/format_ux_review_2026-06-20.md`: no user base, nothing to migrate → backward-compat is NOT a constraint; this is a breaking redesign done as ONE atomic change on `fr-61-65-impl` before it merges. The engine consumes the friendly shape directly (no translation shim); the runtime-record vocabulary — manifest/result/heartbeat/dispatch-envelope `task_id`/`target_repo`/`dispatch_mode` and the `{TASK_ID}`/`{TARGET_REPO}` placeholder tokens — is unchanged (the A2A runtime identity). `schema_version` is a clean break to `"2"`.*

- FR-66: **One mode-discriminated `jobs` format.** A plan is a single `jobs` list; each job carries `id`, `repo`, and a `mode` discriminator ∈ `{agent, command, log, pipeline, shell}`, plus that mode's one field: `agent`→`prompt`/`prompt_file`; `command`→`command` (engine synthesizes the heartbeat wrapper, doneness = exit code); `log`→`log_path` (+ optional `command`/`success_regex`/`failure_regex`/`sentinel_file`/`pid`); `pipeline`→`steps` (each step its own `mode` + optional `gate`); `shell`→`command` (the raw argv; the operator wires `{HEARTBEAT_PATH}` — the advanced escape hatch kept visibly distinct so the false-stall footgun stays legible). `tick.py --check` enforces the per-mode required field and agrees field-for-field with `plan.schema.json` (one source of truth). **Supersedes/retires** the FR-40/41 `adapter:"wrap"|"tail"` selector (folded into `command`/`log` modes) and the `dispatch_mode:"subagent"|"shell"` plan enum (folded into `mode`); the engine's internal wrap/tail synthesis + subagent/shell dispatch are unchanged, only the discriminator the plan declares changed. The per-mode `command` contract is stated in the schema field descriptions + TOOLKIT.md.
  *Acceptance pins:* every mode passes `--check` and dispatches end-to-end (agent unit + command/log/pipeline/shell integration scenarios that FAIL if the engine mis-reads the mode's fields); `agent` w/o prompt, `command` w/o command, `log` w/o log_path, `pipeline` w/o steps, and an unknown `mode` each FAIL `--check` (mutation-verified per-mode branch).
- FR-67: **Strict keys + sanctioned annotation.** `additionalProperties:false` at plan/job/step rejects a typo'd key at `--check` (not silently at dispatch); a `description`/`_comment` field is sanctioned at every level for inline annotation.
  *Acceptance pins:* a job with `promt`/`comand`/`reepo` FAILS `--check` (mutation-verified: removing the strict-keys check lets the typo pass); a plan/job/step carrying `description`/`_comment` validates clean.
- FR-68: **Auto-injected placeholder preamble (format-only).** For `agent` jobs and `agent` pipeline steps, the engine auto-injects the `{HEARTBEAT_PATH}/{TASK_ID}/{RUN_DIR}/{TARGET_REPO}/{HARNESS_BIN}` preamble at dispatch (idempotent — never doubled), so authors write a bare prompt and never hand-type placeholders (FR-21a). `--check` no longer requires the placeholders in an agent prompt (they are injected) but still rejects an unknown `{TYPO}` token. The placeholder TOKENS are unchanged. *(This is the format-only slice; the deeper fully-opt-out heartbeat CONTRACT is FR-70, a follow-up, NOT implemented here.)*
- FR-69: **Capability-preservation.** Every field the prior `fr-61-65-impl` schema carried maps into a mode and is preserved — `prompt_file`, `steps`, `vars` (plan+job+step merge), gates/`outcomes` (the engine-read map folded into the schema), `heartbeat_path`, `auth_check`, `success_regex`/`failure_regex`/`sentinel_file`/`pid`, `adapter_activity_patterns`, `keepalive_seconds`, `measurement`/`allow_reasoning_gates`. The collapse reorganizes; it drops no capability. An optional plan-level `defaults` map is merged under each job.
  *Acceptance pins:* a fixture per preserved field validates under its mode (executable proof the collapse lost nothing — `test_check_plan.py::CapabilityMappingCoverage`).

- FR-70 **(stub — follow-up, NOT implemented in instr 004):** **Fully opt-out heartbeat contract (`heartbeat: auto`).** Reach the dispatch/worker handshake so heartbeating is a fully opt-out CONTRACT (not just an auto-injected prompt preamble) — e.g. a `shell` job could declare `heartbeat: auto` and have the engine guarantee the keepalive without the operator wiring `{HEARTBEAT_PATH}`. Filed so it isn't lost; build status **PLANNED** (§9). Also tracked: the **cross-repo QPB break** — QPB emits/consumes arunner plans, so the rename breaks the QPB side; the required follow-up is a QPB-repo task that MUST begin with its own complete sweep for arunner-plan-key usage (the instr-004 hit list is partial/non-exhaustive).

## 6. Nonfunctional requirements

- NFR-1 **Cross-platform:** Windows, macOS, Linux — equal support; no platform-conditional features in the core.
- NFR-2 **No privileges:** no admin/root anywhere — install, run, schedule (rung 3 exists precisely for the no-scheduler case).
- NFR-3 **Zero runtime dependencies:** tick engine, ticker, and heartbeat helper are Python stdlib only. The only external requirements are user-level Python 3 and (for agent workers) a host CLI.
- NFR-4 **Model-tier tolerance:** orchestration must work on low-reasoning models — all decisions live in the deterministic script; the agent's per-tick role is ~7 fixed steps. Evidence: Haiku 4.5 clean loop (2026-06-11); prose carries absolute paths and dual tool-names because small models will not search or infer.
- NFR-5 **Bounded context:** per-tick agent prose is small and fixed; worker returns are one line; the orchestrator's context grows O(ticks), not O(work). No long-lived in-context state.
- NFR-6 **Determinism & idempotency:** same disk state ⇒ same tick outcome; re-entry is always safe (the property every rung of the ladder leans on).
- NFR-7 **Encoding safety:** all text I/O is `encoding="utf-8", errors="replace"` for external content; console output is ASCII-safe (cp1252 hazard); enforced by AST sweep tests with mutation-verified pins.
- NFR-8 **Reliability through degradation, honestly stated:** wakeup primitives are known to drop silently (observed twice, 2026-06-11); the design treats every rung as fallible and makes recovery one printed command. Docs state the window-stays-open and orphan-on-STOP semantics plainly.
- NFR-9 **Auditability:** any run reconstructable from its run-dir alone — no chat scrollback required.
- NFR-10 **Run-dirs on local disk** (synced/network folders unsupported, warned).
- NFR-11 **Security posture:** no network calls of its own, no telemetry, no shell-out except the operator-declared `worker_cmd` templates; prompt-files avoid shell-quoting injection surfaces; Apache-2.0.
- NFR-12 **Honest host-support claims:** the README's support table distinguishes VERIFIED (evidence-linked) from DESIGNED (unverified) per host/rung. No claim ships without a validation-matrix run behind it. As of v0.1.0 the locked-down floor (cadence 2/3/4 + shell dispatch + Windows) is DESIGNED, not yet VERIFIED (§9) — it must not be claimed as working until its validation matrix is green.
- NFR-13 **Tick cost:** a tick is sub-second script execution; idle ticks are cheap by design so over-polling is always safe.

## 7. Constraints & assumptions

- C-1: One writer per heartbeat file (per run-NN/) — concurrency safety derives from layout, not locking.
- C-2: In-session subagents do not survive their session's turn ending in externally-ticked contexts — hence the cadence/dispatch coupling rule.
- C-3: The orchestrator session (rung 1) or ticker window (rung 3) must stay open for the plan's duration; rung 2 has no such constraint.
- C-4: Workers must be able to run the heartbeat helper (user-level Python 3 on their host).
- C-5: In-session autonomous loops have a host-side fragility that is NOT the timer: per the 2026-06-12 Class-C forensics, wakeups fired 4/4 reliably, but the wakeup-resumed model turn intermittently mis-serializes its first tool call into the text channel and dies silently on `end_turn`. Compaction/E7 was refuted as a cause (0/4). Treat every rung-1 resumed turn as fallible; the FR-26a safety tick is the standing mitigation, independent of root cause.
- C-6: In-context mode (FR-46) and harness ticks time-share a single agent turn — sequential, never concurrent — so monitoring is coarse-grained during in-context bursts by construction. Background workers + subagents + in-context work coexist in one session (the superset); doing in-context work *concurrently* with monitoring is not possible and not needed.
- C-7: In-context mode (FR-46) is rung-1 only — the in-context work requires an agent. The deterministic floor (ticker/cron) can rescue only the harness/background portion of an in-context session, never the in-context tasks themselves (C-5 applies to those).

## 8. Out of scope (this release)

Kill semantics for in-flight workers on STOP (documented orphan behavior — though FR-39 `CANCEL` now frees a stalled run's slot); per-phase stall thresholds; A2A/cross-machine transport (schemas are A2A-ready by carrying `task_id`/`schema_version`); full Codex/Cursor per-host validation matrices (v0.2); harness resume/iterate strategies (deferred); the silent-drop watchdog (`--max-quiet`) beyond the printed-command recovery; orchestrator-side telemetry/dashboards (the table and the disk are the UI); **automatic recovery of the in-context queue from a Class-C drop** (C-7 — no clean auto-relaunch of a fresh agent session exists; recovery stays operator-re-bootstrap).

## 9. Validation evidence map

| Claim | Evidence |
|---|---|
| Autonomous multi-tick loop, idle ticks survive | Spike PASS ×3 passes (Sonnet), `spike-evidence.md` |
| Pool + staggered dispatch + multi-entry | Item-11 E2E, run-dir `20260611T191325Z` records |
| Low-reasoning-model orchestration | Haiku 4.5 run `20260611T231408Z` (re-test in flight) |
| Detached workers outlive dispatch turn | Instruction-003 dry-run (pgrep + heartbeat evidence) |
| Agent honors STOP from prose | Spike pass 3 (STOP mtime vs tick time, state untouched) |
| Idempotency / cycle-only re-tick | 002 smoke + 003 dry-run + unit suite (mutation-verified) |
| Encoding/cp1252 safety | 007/008 AST sweeps, mutation-verified incl. independent orchestrator re-run |
| In-session loops can die silently (NOT the timer: Class-C resumed-turn tool-call mis-serialization; wakeups fired 4/4; E7/compaction refuted) | Instruction-011 transcript forensics, 4 drops root-caused with quoted evidence (`runner/1.5.9/outputs/011-loop-drop-self-forensics.md`); ladder + FR-26a safety-tick rationale |
| Cadence 2/3/4, shell dispatch, Windows floor | **PENDING** — needs a *recorded* cross-platform matrix run (NFR-12): operator-run Windows V-7/V-8 (Task Scheduler + foreground ticker) and the V-10 safety-tick. macOS cadence-2 cron (V-9) and rung-3 ticker are VERIFIED in the README support table, but the **Windows / full-matrix** floor claim stays PENDING — **dogfooding / always-on runs do not satisfy this row** (they measure survival, they do not validate a floor matrix). |
| Canonical version + startup banner (FR-34) | **VERIFIED** — `test_version_single_source.py` (value-agnostic drift test, mutation-verified to bite; installed `arunner --version` → `0.1.0` from the wheel). v0.1.0 iter 1 + 13a (`runner/1.5.9/outputs/018-iteration-1-test-scaffold-and-version.md`, `034-13a-packaging-and-publish-safety.md`). |
| Control-file convention: PAUSE/RESUME, CADENCE, POLL-NOW, CANCEL (FR-35..39) | **VERIFIED** — `test_control_files.py`, `test_cadence_pool.py`, `test_poll_now.py`, `test_cancel.py` + integration scenarios `pause_blocks_dispatch` / `resume_continues` / `poll_now_collapses_cadence` / `cancel_shared_state`. v0.1.0 iters 2–5 (`runner/1.5.9/outputs/019`, `021`, `022`, `023`). |
| Flexible-input adapter: wrap-and-run (stdout capture) + tail-existing (FR-40/41) | **VERIFIED** — `test_wrap_adapter.py`, `test_tail_adapter.py` + scenarios `wrap_adapter_completes` / `tail_adapter_completes` / `wrap_output_keyword_no_misreap` (the status-by-JSON-field fix for the substring-misreap engine bug found in review). v0.1.0 iters 7–8 (`runner/1.5.9/outputs/025`, `026`). **SUPERSEDED at the plan surface by FR-66 (instr 004):** the `adapter:"wrap"/"tail"` selector is folded into `command`/`log` modes — the engine's wrap/tail synthesis is unchanged, only the plan discriminator changed. |
| Plan pre-flight `--check` + job shorthand + examples + TOOLKIT plan-builder (FR-42..44) | **VERIFIED** — `test_check_plan.py`, `test_jobs_shorthand.py` (examples anti-drift binding), `test_toolkit_examples.py`. v0.1.0 iters 6/9/10 (`runner/1.5.9/outputs/024`, `027`, `028`). |
| SUMMARY roll-up at done (FR-45) | **VERIFIED** — `test_summary.py` (guarded write on the done-transition). v0.1.0 iter 11 (`runner/1.5.9/outputs/029`). |
| In-context dispatch-to-self (3rd dispatch mode, orthogonal to cadence) + streaming instruction queue + ERR + monitoring-pause visibility (FR-46..49, UC-9) | **VERIFIED** — `test_incontext.py` (selection logic incl. the STOP-halts-mid-queue negative; monitoring-pause render). v0.1.0 iter 12 (`runner/1.5.9/outputs/030`). |
| Deterministic ticker-driven integration-test suite w/ independent disk assertions (FR-51) | **VERIFIED** — 11 scenarios under `tests/integration/scenarios/` driven by `ticker.py --once`; `test_integration_scenarios.py` + `test_checker_independence.py` (the independent stdlib checker, enforced mechanically). v0.1.0 iter 2 settle fix (`runner/1.5.9/outputs/020`). |
| Unattended-reliability positioning (FR-50) | **VERIFIED** — README + TOOLKIT steer unattended runs to the deterministic ticker/cron rungs; the in-session agent rung is framed as interactive + the FR-26a safety tick, not the reliability path; `test_positioning_honesty.py` guards the framing mechanically. v0.1.0 iter 13b (`runner/1.5.9/outputs/035`). |
| Cross-agent universality positioning + honest engine/floor-vs-per-host split (FR-54) | **VERIFIED** — README/SKILL/TOOLKIT lead with the cross-agent identity; the support table separates VERIFIED **workers** (Copilot/Codex, macOS rung 3, V-14) from the **orchestrator** role (FR-52 builder: DESIGNED any host, VERIFIED Claude Code only); `test_positioning_honesty.py`. v0.1.0 iter 13b (`runner/1.5.9/outputs/035`). |
| Interactive session builder: describe → preview → run → persist (both shorthand + expanded) → incremental edit (FR-52, UC-10) | **VERIFIED** (capability) — `test_preview.py` (per-job dispatch render, mutation-pinned), `test_cli_journey.py` (E2E describe→preview→run→persist). Host-agent-driven, embeds no model; the **"any host" orchestrator** claim is DESIGNED / VERIFIED Claude-Code-only (README support table). v0.1.0 iter 13/033 (`runner/1.5.9/outputs/033`). |
| First-class lifecycle verbs: new / run / status / resume / summary (FR-53) | **VERIFIED** — `test_cli.py` + `test_cli_journey.py` (E2E real user journey); the three console scripts ship in the wheel (13a). v0.1.0 iter 13/032 (`runner/1.5.9/outputs/032`). |
| Continuation contract: externalized per-tick continue/stop verdict (closed halt set) + yield accounting + abandonment / illegitimate-yield / false-halt-claim detector (FR-55, UC-11, US-11) | **VERIFIED** (mechanism; post-hoc not prevention) — `test_continuation.py` (the verdict is a pure function of disk state across the whole closed set + the blocker lifecycle + STOP-tick read-only; the mid-run-CONTINUE pin is mutation-verified) + the `continuation_contract` integration scenarios (`continuation_{honor,abandon,crash_then_resume,false_yield,false_halt_claim,scheduled_gap,blocked_then_clear}`) driven by the deterministic ticker and graded by the **independent** stdlib checker's three-class detector (`test_integration_scenarios.py::test_continuation_detector_discriminates` proves it FIRES on a real abandonment and stays silent on an honored run). v0.1.0 iter (`runner/1.5.9/outputs/036-fr55-continuation-contract-build.md`). **Honest limit (NFR-12):** the stub-host scenario pins the mechanism, not a real LLM's discretion, and catches abandonment post-hoc; a live audit (arunner driving a real job, the same checker over its journal) is measurement, not a deterministic gate. |
| Activity-pattern extraction for wrap/tail adapters (operator-declared relevance; display-only, not doneness) (FR-56, UC-12, US-12) | **VERIFIED** — `test_activity_patterns.py`: the matcher (most-recent-match [pinned], staleness age hint [pinned], `(running...)` placeholder, line truncation, the per-scan byte ceiling), `--check` validation (non-list / non-string / empty / >16 / uncompilable / complexity-screen + the success/failure compile retrofit), both-adapter synthesis, and the **display-only firewall** (a never-matching pattern leaves doneness intact [pinned]); the `EndToEnd` tests drive the REAL wrap + tail subprocess (grace 0) and assert the ACTIVITY label is the relevant line, not the noise; the `sim_tail_log_noise` integration scenario carries `adapter_activity_patterns` through the engine synthesis to `done`. ReDoS bounds (per-line truncation + per-tick byte ceiling + complexity screen + ≤16 cap) are reduced-and-disclosed (NFR-11). v0.1.0 iter (`runner/1.5.9/outputs/038-fr56-activity-pattern-extraction.md`). |
| Live enqueue: grow a running batch's queue with a first-class `add` verb, stage-and-absorb (FR-57, US-13, UC-1) | **VERIFIED** — `arunner add` → `incoming/` (validated by `--check` pre-gate, never touches live files); the tick absorbs under `.tick.lock`, append-only `run-NN`, placeholders unresolved, no `done` write; `test_live_enqueue.py` (9 tests, 2 PINs: append-only numbering, add-never-touches-live-files) |
| Frequent activity-refresh cadence — configurable `--keepalive-seconds` decoupled from stall, first-scan-at-start, synthesized into the adapter (FR-58a, US-14, UC-2) | **VERIFIED** — `heartbeat.py` `--keepalive-seconds` (plan field, ~45s default, decoupled from stall/3) + `_Keepalive.emit_first` + `_adapter_worker_cmd(entry, plan)` synthesizes grace/stall/keepalive (the previously-inert knobs); `--check` rejects keepalive>grace; `test_activity_cadence.py` (11 tests, 5 PINs) drives the DEFAULT grace path |
| Regular visible table — orchestrator prints `status_table` each tick, non-suppressible (FR-58b, US-14, UC-2) | PENDING (per-host agent-rung, DESIGNED, NFR-12) — SKILL/runbook hardening of FR-27; graded separately from FR-58a |
| Read-only disk monitor: rung-independent `arunner monitor` sidecar renders the table from externalized state, never writes (FR-59, US-15, UC-2) | **VERIFIED** — `arunner monitor <run-dir> [--interval/--once/--no-clear]` reuses `_format_table` over disk-loaded `harness_status.json`/`plan.json`, strictly read-only (no write / no `.tick.lock` / no control file / no tick); monitor-owned freshness "as-of last tick" line; ANSI clear + `--no-clear` (no `curses`/`os.system`); read-tolerance skips a bad frame; terminal/`--once`/Ctrl-C exit clean. `test_monitor.py` (12 tests, 3 PINs: never-writes, renderer-reuse-no-fork, freshness-split — all mutation-verified). Per-OS recorded run stays the operator's action |
| Chat ⇄ runner message channel: typed inbox/outbox, acknowledged, idempotent, closed verb set (FR-60, US-16, UC-13) | **VERIFIED** — inbox drained under `.tick.lock` at tick start (gated behind STOP read-only); processed-ids ledger (mark-first, crash-safe replay = no-op); append-only outbox ack (`received`→`applied`/`rejected`) + `result` (id↔task_id on completion); closed verbs enqueue/control/dispatch-job/run-batch/snapshot/note; `--check` rejects malformed/bad-entry before landing; read-only verbs never-writes-pinned; no network listener; `arunner msg`/`outbox` CLI. `test_message_channel.py` (11 tests, 2 PINs: idempotency-ledger, read-only-never-writes — mutation-verified) |
| Prompt-from-file with light `{var}` templating: `worker_prompt_file` exactly-one-of inline, plan-dir-relative, snapshotted at `--init`, designated replace-key pass preserving literal braces (FR-61) — **arunner 0.2.0** | **VERIFIED** — `worker_prompt_file` snapshotted into `run-NN/prompt.snapshot.md` at `--init` (mutation-verified authoritative: a source mutated after init never changes the run), plan-dir-relative resolution from a non-cwd dir, designated-key `{var}` pass (literal `str.replace`) run BEFORE the reserved placeholders that does NOT scan stray braces (literal single-brace JSON survives — Council FIX-2 PIN); `--check` enforces the `plan.schema.json` `oneOf` (exactly-one-of worker_prompt/worker_prompt_file/steps/adapter) and rejects a reserved `vars` key or a value carrying a reserved token. `test_prompt_from_file.py` (13 tests, 2 mutation PINs). instr 002. |
| Multi-step entries: ordered sub-runs under `run-NN/steps/step-MM/`, one pool slot per entry, `step_index`/`step_count`, resume-without-re-run (FR-62) — **arunner 0.2.0** | **VERIFIED** — `steps:[...]` run sequentially in ONE pool slot (`_holds_slot` keeps the slot across the sequence — mutation-verified the second entry waits under `pool_size:1`); each step a full FR-18 sub-run at `run-NN/steps/step-MM/` (own heartbeat/manifest/result, manifest carries `step_index`/`step_count`); dispatch-one-step-per-tick, advance on clean terminal; resume reaps-not-re-runs completed steps (mutation-verified no re-run) and a crash between reap and next-dispatch resumes; a failed step terminals the entry with the per-step failure; table shows `sN/M`, SUMMARY lists per-step statuses. `test_multistep.py` (10 tests, 2 mutation PINs). instr 002. |
| Continuation gates between steps: deterministic `shell` gate (exit-code only) default + fenced `reasoning` gate (separate judging context, structured verdict, opt-in/excludable), recorded `step-MM/gate.json` read-on-resume (FR-63) — **arunner 0.2.0** | **VERIFIED** — shell gate runs argv **with `cwd` = the step/entry `target_repo`** (so a relative-path/relative-import gate never false-fails from the orchestrator's incidental cwd — mutation-verified by a sentinel test; instr 003) → maps EXIT CODE to outcome (exit-code only; no stdout read), recorded to `step-MM/gate.json` and READ on resume (mutation-verified the argv runs exactly once — no recompute, NFR-6); reasoning gate dispatched as a DISTINCT judge sub-run (`run-NN/steps/step-MM/gate/`) reading a structured `data.verdict`, identity recorded, malformed/absent verdict → `internal_error` fail-closed; `--check` rejects a reasoning gate without `allow_reasoning_gates:true`, in a `measurement:true` run, with a same-context judge, or without a distinct judge prompt (upholds FR-51). Graded by the independent stdlib checker (no LLM). `test_gates.py` (14 tests, 3 mutation PINs). instr 002 + 003. |
| Gate outcome vocabulary (closed set): `continue`/`halt`(→FR-55 verdict)/`skip-to-next`/`behavior-flag:<name>`/`internal_error`, recorded verbatim (FR-64) — **arunner 0.2.0** | **VERIFIED** — each closed-set outcome drives sequencing and is recorded verbatim in `gate.json`: `continue` advances; `halt` → an existing FR-55 `failed` terminal (entry result `FAILED`); `skip-to-next[:step-MM]` synthesizes auditable `SKIPPED` step terminals; `behavior-flag:<name>` continues AND exposes the operator-declared flag as the next step's `{var}` (verified substituted into the next dispatch); an out-of-set outcome is coerced to `internal_error` fail-closed (mutation-verified). `test_gates.py` (with FR-63). instr 002. |
| Per-run and per-sub-run token reporting: `data.usage = {input_tokens, output_tokens}` → top-level result fields, additive roll-up, TOKENS column + SUMMARY, graceful `—` degradation, never control-flow (FR-65) — **arunner 0.2.0** | **VERIFIED** — the engine reads EXACTLY `data.usage = {input_tokens, output_tokens}` off heartbeats into top-level `result.schema.json` fields at reap (single-prompt + per-step), with additive per-step→entry→run roll-up (mutation-verified the sum is additive, not last-wins); surfaced via the status-table/monitor TOKENS column + SUMMARY (`SUMMARY.md` + `summary.json`). Honest degradation (NFR-12): no usage → `—` (mutation-verified never a fabricated `0`/`0/0`), partial → `partial (N of M jobs reported)`, malformed `data.usage` → skipped-with-warning (non-fatal); an `IN_PROGRESS` `data.usage` never makes a run `done` (reporting-only). `test_tokens.py` (6 tests, 2 mutation PINs). instr 002. |
| One mode-discriminated `jobs` plan format: single `jobs` list + `mode` ∈ {agent,command,log,pipeline,shell}, per-mode required enforced at `--check` == schema; folds/retires FR-40/41 `adapter` + the `dispatch_mode` plan enum into `mode` (FR-66) — **arunner 0.2.0** | **VERIFIED** — the engine reads the friendly `jobs`/`id`/`repo`/`mode` shape directly; `check_plan` enforces per-mode required + agrees field-for-field with `plan.schema.json` (schema_version `"2"`); runtime-record vocabulary (manifest/result/heartbeat/dispatch-envelope `task_id`/`target_repo`/`dispatch_mode` + `{TASK_ID}` tokens) unchanged. Every mode dispatches end-to-end: integration scenarios `wrap_adapter_completes`/`sim_wrap_log_noise`/`wrap_output_keyword_no_misreap` (command, engine-synthesized wrap), `tail_adapter_completes`/`sim_tail_log_noise` (log, engine-synthesized tail), the stub-worker `continuation_*`/`pool_*`/`cancel_shared_state`/… (shell), and the new `pipeline_gate_completes` (pipeline + gate reading `outcomes`); agent dispatch is unit-pinned (`test_tick.py` — the in-session rung is not ticker-drivable by design, FR-54). Per-mode-required + unknown-mode mutation pins in `test_check_plan.py`. instr 004. |
| Strict keys + sanctioned annotation: `additionalProperties:false` typo rejection + `description`/`_comment` at plan/job/step (FR-67) — **arunner 0.2.0** | **VERIFIED** — `check_plan` rejects an unknown job/step key (a typo like `promt`/`reepo`) in lockstep with the schema; `description`/`_comment` validate at every level. `test_check_plan.py::StrictKeysTypoRejection` (mutation-verified: dropping the strict-keys loop lets the typo pass) + `::AnnotationSanctioned`. instr 004. |
| Auto-injected placeholder preamble (format-only) for `agent` jobs/steps; `--check` drops the placeholder-required rule but keeps the `{TYPO}` catch (FR-68) — **arunner 0.2.0** | **VERIFIED** — the engine prepends the `{HEARTBEAT_PATH}/{TASK_ID}/{RUN_DIR}/{TARGET_REPO}/{HARNESS_BIN}` preamble at dispatch (idempotent, never doubled); a bare agent prompt is `--check`-clean and the dispatched `worker_prompt` carries the resolved preamble; the placeholder TOKENS unchanged. `test_check_plan.py::test_agent_bare_prompt_clean_no_placeholder_required` + `test_tick.py` (dispatch prompt resolution). instr 004. |
| Capability-preservation: every prior `fr-61-65-impl` field maps into a mode; optional plan `defaults` merged under each job (FR-69) — **arunner 0.2.0** | **VERIFIED** — a fixture per preserved field validates under its mode (`prompt_file`, `steps`, `vars` plan+job+step, gate `outcomes` folded into the schema, `heartbeat_path`, `auth_check`, `success_regex`/`failure_regex`/`sentinel_file`/`pid`, `adapter_activity_patterns`, `keepalive_seconds`); `defaults` merge at `--check`/`--init`. `test_check_plan.py::CapabilityMappingCoverage` + `::DefaultsMerge`. instr 004. |
| Fully opt-out heartbeat contract (`heartbeat: auto`) reaching the dispatch/worker handshake (FR-70) + the cross-repo QPB plan-key follow-up | **PLANNED** — filed by instr 004 as a follow-up, NOT implemented here (instr 004 ships only the format-only preamble auto-injection, FR-68). The QPB break (QPB emits/consumes arunner plans) is a required QPB-repo task that MUST begin with its own complete sweep — the instr-004 hit list is partial/non-exhaustive. |

---

*End of requirements v1.4. Functional/nonfunctional numbering is the stable reference for instructions, tests, and the eventual README. FR-34..50 + UC-9 are the v0.1.0 first-release scope (build status per-row in §9 — most VERIFIED as of v0.1.0; the cadence/Windows floor matrix stays PENDING); FR-51..55 + UC-10/11 are the v0.1.0 testing, UX, and autonomy-integrity additions; FR-56 + UC-12/US-12 is the adapter activity-extraction addition (VERIFIED); FR-57..59 + US-13/14/15 are the live-ops additions (live enqueue + monitoring cadence + read-only monitor VERIFIED; FR-58b in-session-table-print stays PENDING/DESIGNED); FR-60 + US-16/UC-13 is the chat⇄runner message channel (VERIFIED). v1.4 (2026-06-18) adds FR-61..65 — multi-step / gated-pipeline / token-reporting, targeting **arunner 0.2.0** — VERIFIED (instr 002 implementation; the engine version bumps to 0.2.0 when the release ships).*
