# Instruction 030 self-Council synthesis — Iteration 12: in-context worker mode (FR-46..49)

*Mandatory 3-panel worker self-Council (the last feature iteration; the only piece whose core is agent behavior, so honesty-sensitive). Three independent fresh-context panelists, role-locked, live-verified; mutation bites used scratch copies — the real source was never mutated. Synthesized by the runner worker. Date: 2026-06-13. Committed into the wakecycle repo at `reviews/030_self_council/`.*

## Panelist verdicts
| Panelist | Charter | Verdict |
|----------|---------|---------|
| A — `panelist_A_selection.md` | Selection / queue correctness | **SHIP** |
| B — `panelist_B_engine_fit.md` | Engine-fit & bounded context | **SHIP** |
| C — `panelist_C_honesty.md` | Honesty (adversarial) | **SHIP** |

**Outcome: unanimous SHIP, no FIX-REQUIRED.**

## Where the panelists confirmed (live, highest confidence)
1. **Selection / queue correct + deterministic (A).** Lowest-numbered `NNN-` chosen; processed-skip matches by **normalized integer stem** (not filename — `002-b.md` is skipped by output `002-DIFFERENT.md`); zero-padding normalizes (`009` < `010`, bare `9` matches `009`); idle (None) when all processed / empty; non-numeric files ignored. **STOP halts mid-queue** (read-only). Both mutation pins bite: dropping the STOP guard picks `002` under STOP; ignoring the `done` set re-picks a processed instruction. Adversarial cases (ties, numbering gaps, a 50-digit number, missing dirs, an instruction-that-is-a-directory) stayed deterministic and never crashed. Non-blocking note: a *directory* named `NNN-` in outputs doesn't count as "processed" — spec-consistent (outputs are files).
2. **Engine correctly untouched; irregular-tick tolerance accurate (B).** `tick.py`/`ticker.py`/`heartbeat.py` are byte-for-byte unchanged; the only code addition is `bin/incontext.py`. The FR-46 claim that the engine already tolerates the irregular ticks in-context produces is accurate and pinned: the **E2 suppress-stall path** makes a long in-context gap observationally identical to a slept machine (regression-tested by `test_wall_clock_jump_suppresses_stall` + a normal-cadence control). The deterministic core is a stateless pure disk scan; `monitoring_pause_note` is deterministic (no internal wall-clock read) and ASCII-safe; **ERR (FR-48) is honestly placed in the SKILL as agent behavior**, not faked as engine logic. Adversarial edge — launch-grace not E2-suppressed under a very long pause — is pre-existing, spec-acknowledged (REQUIREMENTS §FR-49/limitation), agent-side, not a regression.
3. **Honesty holds, falsifiably (C).** All four surfaces (README, TOOLKIT, SKILL, `bin/incontext.py` docstring) carry the C-7 disclaimer with **zero over-claims** — an adversarial regex sweep found every "fixes Class-C / auto-recover / immune / deterministic" hit is either a negation or correctly attributed to the deterministic *rungs*, not in-context mode. The cross-axis coupling (in-context tasks are rung-1-only; the floor rescues only the background portion) and ERR-as-agent-discipline are **stated, not implied**. The honesty tests are genuinely falsifiable (scratch experiment: injecting an over-claim fails `test_no_class_c_overclaim`; deleting a disclaimer fails `test_c7_limitation_present_everywhere`), and the `_norm` whitespace-collapse correctly rescues a line-wrapped disclaimer.

## Single verdict: **SHIP**
In-context is the third dispatch mode: the deterministic core — the FR-47 streaming-queue selection (lowest unprocessed; STOP halts mid-queue) and the FR-49 monitoring-pause note — is real, pure-disk-state code, unit-tested and mutation-pinned. The agent-behavior parts (doing the work; the FR-48 ERR rehydrate-on-resume discipline) are documented in the SKILL and honestly framed as dogfood, not unit assertions. The engine needed no change (E2 already tolerates the irregular ticks). Most important: the docs are scrupulously honest — in-context mode does NOT fix Class-C, has no queue auto-recovery, is rung-1-only, and is NOT the unattended-reliability path; that limitation appears in all four surfaces and is enforced by a falsifiable test. Suite 164→176.
